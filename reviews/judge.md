## Round 1 - JUDGE, 2026-09-21

Method: read all briefs + 3 reviews, re-ran everything myself, wrote 6 perturbation harnesses (`reviews/judge_scratch/*.py`, scratch data in `reviews/judge_scratch/data/`), drove the UI in headless Chrome 153 over CDP (Claude-in-Chrome extension not connected) in REPLAY and LIVE mode (real uvicorn + Vite proxy, output dir copied to scratchpad so `backend/out` state untouched). Screenshots: `reviews/shots/judge-*.png`. Dev servers + Chrome stopped, ports 8000/5311 verified free. Originals spot-checked: infopack.docx text and use-case PDF (4 pages) match the `work/*.txt` extracts (incl. "No mismatch detected", scanned docs, "Handle processing failures visibly").

### 1. Independent verification (numbers)

| Check | Result |
|---|---|
| `cd backend && pytest -q` | 62 passed (7.6 s) |
| `python -m sdoc.run_all` | engine=rules, 520 emails, 17.8 s; 220 BL_COMPARISON / 125 SI / 75 INV / 60 GEN / 40 SPAM; 454 OK / 46 MISMATCH / 20 NEEDS_REVIEW |
| `score_cli.py backend/out/submission.json` | **FINAL 1.0000** (stage1 macroF1 1.000, stage3 defect-F1 1.000, e2e 46/46, escalation P=R=1.00, 5/5 per reason). Claim CONFIRMED. (Scorer needs `PYTHONUTF8=1` on Windows cp1251 console, unicode bars crash it - not a product bug.) |
| submission shape | 520 keys == inbox ids == `sample_submission.json` keys; every entry has exactly `category,status,review_reason,has_defect,defect_fields`; non-comparison -> `OK` as in sample. |
| label leakage grep | no read of `ground_truth.json`, `data_v2`, generator, or any `email_NNN` literal in `backend/sdoc` or `frontend/src`; replay/dist contain only one offline number (`accuracy_vs_labels`). `tools/score_local.py` (dev-only) is the only consumer of the scorer. CLEAN. |
| `tsc -b` + `vite build` (to scratch outDir) | 0 TS errors, JS 321 kB (102 kB gzip), 6.3 MB with replay; no ground-truth strings in the bundle. |
| REPLAY drives UI | YES. `index.json` (list, rows carry `result`), `traces/<id>.json`, `stats.json`, `flybrain.json` (1600 KC, 32 inputs, `input_names`, `calibration`) all consumed; sidebar 520/114/72/100%, graph lights per trace, report table, fly panel 1600 KC. **No shape mismatch between backend export and frontend adapters (previous "mock-verified only" risk retired).** |
| LIVE drives UI | YES. `/api/health` auto-detect, SSE via proxy, streamed lighting, `POST /api/review` (backend log 200, real `flybrain.history` entry with `simulated:false`), stats sidebar 20 -> 19 review after my confirm. API retry verified: `inject_fail=field:consignee,field:port_of_loading` -> two red nodes, NEEDS_REVIEW; `/api/retry` re-runs only those 2 + aggregator/compare/gate -> MISMATCH on consignee/notify. |

**Anti-overfit hunt: a perfect 1.0 on the synthetic set IS partly overfit. Evidence below.** The pipeline generalises fail-safe (mostly degrades to NEEDS_REVIEW, rarely silently wrong) but has real false alarms on messier input.

### 2. Robustness results (my own perturbations of 40-84 txt SI/BL pairs re-run through the real pipeline)

Semantics-preserving (expected: unchanged status). "esc" = degraded to NEEDS_REVIEW/missing_value (fail-safe but the real defect is lost); "FA" = false MISMATCH.

| Perturbation | Result |
|---|---|
| tab / lowercase labels / `Label :   v` / bullets `* L: v` / CRLF / value on next line / blank lines / BOM+trailing ws / upper- or title-case values / double spaces / shuffled lines / preamble+footer / look-alike extra fields (Net/Tare/Container No) | 0 changed. Good. |
| separators `Label - value`, `Label = value`, markdown `\| Label \| value \|` | 84/84 -> esc (all 7 fields "missing") |
| docx 4-column tables (2 label/value pairs per row), transposed docx/xlsx tables (header row of labels + value row) | 84/84 -> esc |
| docx paragraphs, docx 2-col table, xlsx A/B, xlsx B/C, xlsx numeric weight, pdf lines, pdf columns, mixed txt/pdf, mixed docx/xlsx | 0 changed. Good. |
| image-only PDF pair | 84/84 -> NEEDS_REVIEW/unreadable (never read; no OCR/vision path) |
| unseen label synonyms (68 tried) | 12 miss (esc): `Shipper Name & Address`, `Shipper / Consignor`, `Sender`, `Consignee / Buyer`, `Buyer`, `Notify Party / Address`, `Loading Port / Place of Receipt`, `Departure Port`, `Final Destination`, `Port of Delivery`, `Containers/Packages`, `Total Cntrs`, `Equipment Count`, `Gross Weight of Cargo`, `Cargo Gross Weight`, `Weight` ... The rest (POL/POD/G.W./Gross Mass/GW/Qty of Containers...) hit. No false alarms from label misses. |
| value normalisation unit run (138 pairs) | 101 as I expected. **False alarms on plausible formatting** (real SI vs BL of the same entity): `SDN BHD` vs `S/B`; `LLC` vs `L.L.C.`; `... LLC` vs `... LLC, DUBAI`; `GMBH` vs `GMBH & CO. KG`; `PT X` vs `X`; `FZ-LLC` vs `FZLLC`; `(S)` vs `SINGAPORE`; `(M)` vs `(MALAYSIA)`; `SINGAPORE (SGSIN)` vs `SINGAPORE SGSIN`; `PORT KLANG, MALAYSIA` vs `PORT KLANG MALAYSIA` / `Port Klang - Malaysia`; **any country not in the hard-coded `_COUNTRIES` list (IRAN, IRAQ, COLOMBIA, ARGENTINA, ECUADOR, `THE NETHERLANDS`, `DJIBOUTI, DJIBOUTI`)** when only one doc carries the country; `JEBEL ALI, UAE` vs `JEBEL ALI PORT`; `40'HC x 6`; `22 MTON`. |
| truncated BL (90% / 70% / 50% / 30%) | 0 / 35 / 40 / 40 of 40 -> esc missing_value. Good, fail-safe. |
| **BL cut mid-number (`Gross Weight: 2`)** | **22/22 clean pairs -> FALSE MISMATCH "SI 21577 / BL 2"**; 11 real-mismatch pairs get a bogus extra weight defect; only 7 (>=3 diffs) caught by the gate. Silent wrong answer, undetected corruption |
| garbage / binary / empty BL | NEEDS_REVIEW unreadable or missing_value. Good. |
| lookalike field `Gross Weight of container: 2,000 KG` added | 40/40 -> gate escalation (conflict flag). Good grey-zone behaviour. |

True-defect detection (expect MISMATCH): weight +1 kg, +10 kg, +500 kg as MT, container +1: detected on every email the gate did not escalate. **One-letter typo in consignee:** of 60 clean pairs 22 escalated by the gate (near-miss), 11 reported MISMATCH (short names have sim < 0.88), so "typo = defect or review?" depends on name length. Swap POL/POD, notify name -1 letter: detected/escalated, none silently missed.

Fresh-phrasing classifier probe (23 emails I wrote in generic shipping-inbox language): **18/23 = 78%**. Misses: "New booking - please raise BL instruction" -> GENERAL; "Statement of account August" -> GENERAL; "Re: Draft BL update / thanks, will review" -> BL_COMPARISON; "Claim your prize ... send bank details" -> GENERAL; phishing "account is locked" w/ lookalike-domain link -> GENERAL. Cause visible in `classifier.py`: sender allow-list `aprilasia|fujitogrp|safqa|psabdp|roxcel|ifpla|algurg|vitalsolutions`, subject regexes for `_RPA_`, `Berthing Report`, `UPDATE SUMMARY`, `MISSING GR`, OC-code pattern `5[A-Z]{3}-\d+`, `happy and prosperous`. The header claims "generic shipping-inbox language, never ... generator"; that is not fully true: these are dataset-derived vocabulary. No email ids or labels are read, so it is not cheating, but it is overfit to this inbox.

### 3. Spec coverage checklist (`usecase.txt`)

| Item | Status | Evidence |
|---|---|---|
| Classify 5 categories | MET (on data) / PARTIAL (generalisation) | 520/520; fresh-phrasing 78% for rules; Gemini path would carry generalisation but is unverified live |
| Extract 7 fields SI+BL | MET | txt/pdf/docx/xlsx parsed; alignment by meaning incl. `Load Port`=`Port of Loading`, CJK-glued labels; brittle beyond dataset layouts (see 2) |
| Compare, side by side, "No mismatch detected" | MET | UI SI-vs-BL table with evidence lines; headline "No mismatch detected"; `container_count: SI 3 / BL 4` style headline verified (email_004 live) |
| Ask for help: escalate with context, evidence, reason | MET | reason enum + human-readable reason + evidence rows + open/resolved state; live-verified |
| PDF and Word attachments, tables | MET | 28 pdf / 8 docx / 22 xlsx parsed; my re-rendered variants pass except 4-col/transposed tables |
| Scanned docs (OCR or vision LLM) | **PARTIAL** | image-only PDFs are detected and escalated as `unreadable` with reason, but never READ; no OCR engine installed, no vision path in `llm.py` (`contents=prompt` text only) |
| Messier inputs (labels, formats, misleading subjects, missing attachments) | PARTIAL | dataset's mess handled (score 1.0); misleading subject handled by attachments+body; my harder variants show ~12 label misses, layout misses, ~25 formatting false alarms (sec. 2) |
| Distinguish real discrepancy from formatting issue | PARTIAL | unit/case/legal-suffix/country/code normalisation good for the seen forms; no false alarms on 0-change perturbations of the listed kinds; fails on unseen legal forms/countries |
| Reliability: unreadable / missing value / uncertain -> review with evidence+reason | MET | 4 deterministic reasons + gate on uncertainty (proved on my perturbations: 40/40 conflict cases, 22/33 typo cases escalated) |
| Human confirm/correct -> report updates | MET (LIVE) / PARTIAL (REPLAY = simulated locally, labelled) | live: `POST /api/review` recomputes with pure compare, closes escalation, list dot + stats update |
| Processing failures visible + retry | PARTIAL | backend: per-node error events, fallback, `/api/retry` re-runs only failed nodes (verified). UI: red node + "Retry failed nodes" exist, but failure can be produced only by API `?inject_fail=`; nothing in the UI triggers it, retry is "simulated" in REPLAY |

### 4. Fly-brain: honesty and value

* Honesty: wording is correct everywhere I looked ("Architecture inspired by the fruit fly's olfactory system", "our own small network"). No FlyWire/connectome claim in UI, README, code. PASS.
* Real on real data: max suspicion 0.0003 over 109 comparisons, 0 gate-only escalations; all 20 NEEDS_REVIEW are deterministic triggers and every one shows the SAME suspicion (0.6321 = 1-1/e for the 13 non-missing-value ones, 0.8854 for the rest). A judge who opens 3 review emails sees an identical number and asks "is it doing anything?". As shipped, the demo path reads as decorative. DEVELOPER's own objection #1/#2 in developer.md is correct and unresolved.
* But the gate is NOT decorative when the input is genuinely grey. My measurements (fresh gate, reproducible: `reviews/judge_scratch/run_e.py`, `run_f.py`): 63/80 near-miss-typo'd consignee pairs and 40/40 pairs with a conflicting look-alike weight line are escalated by the gate on its own; on those data the rules alone would have printed a wrong MISMATCH (typo) or a possibly wrong pick (conflict). **Few-shot correction works and is local:** teaching `escalation_unneeded` on one pattern drops suspicion 0.487 -> 0.234 -> 0.101 -> 0.042, the pattern stops escalating after 3 verdicts (updates 9-54 of 1600 KC), and other near-miss vectors drop from 62/62 to 27/62 escalating. That is exactly the idea.md pitch and it is measurable. It is simply not in the shipped demo/replay data.
* Weak spots: (a) calibration recall 0.995 is measured on synthetic anomalies authored by the same developer from the same 32 input semantics: circular, present as a sanity check only; (b) `many_mismatches` (>=3 diffs) is an anomaly by construction, so a genuinely bad BL with 3+ real defects is escalated and its `defect_fields` are dropped (`has_defect:false`) - for e2e scoring that trades a caught defect for a review; (c) a gate escalation is reported with `review_reason: missing_value` (or `unreadable` when OCR) although nothing is missing; that is a lie in the field the scorer/UI reads; (d) escalation of a MISMATCH email also drops confirmed non-near-miss mismatches (12/15 emails with 1-2 real defects + one typo lost their real defects).
* What would make it demonstrably matter without hurting accuracy: ship a **"messy inbox" grey-zone set** generated by script (the mutations in `perturb.py`/`run_d.py`: typo'd names, conflicting weight lines, 4-col tables, truncated values, OCR-read scans, unseen labels) as extra replay traces, run the same pipeline, and publish a 2x2 table: with/without gate x (false alarms, missed defects, human-review load) + the 3-verdict teaching curve above. Keep the official 520-email number untouched (gate = 0 contribution there, say so). In the UI make a "Grey-zone demo" chip list that opens escalations where the fly panel lights up and the verdict flips future behaviour. Do not present the gate as raising the official score.

### 5. Architecture vs idea.md

| idea.md element | Verdict |
|---|---|
| classifier -> 7 parallel field agents -> aggregator -> pure compare -> fly gate -> escalation | REAL: `pipeline.py` asyncio.gather of 7 per-field tasks each emitting start/done/error; aggregator collates and reports failed agents; `compare` is pure (no LLM); gate is numpy; events are the ones the UI renders (`t_ms`, duration, engine). In rules mode the "agents" are regex twins (labelled `rules`); not an LLM claim. |
| UI graph reflects backend events | YES (verified live SSE and replay events; per-node engine + duration) |
| Gemini path | Exists, structured JSON, evidence verified verbatim, fallback visible. **Unverified live** (fake-client tests only). See objection 7. |
| Fly-brain gate | real net, real learning, interpretable; see sec. 4 |
| Vercel replay | READY: `vercel.json` rewrites exclude `replay/`,`assets/`,`api/`; auto-detect falls back LIVE -> REPLAY -> MOCK in ~1.5 s; build 6.3 MB. In replay, review/retry are simulated + labelled; the Hebbian update uses the real rule on exported weights (INNOVATOR change), so the "unneeded" verdict visibly changes weights; the default click (verdict = "escalation correct" for deterministic triggers) is a no-op: 0.63 -> 0.63, mean weight 1.00 -> 1.00. |

Cost of untested Gemini with judges: HIGH-severity/medium-probability. If a judge drops a key in: 1 classify + 7 field calls per comparison email; free-tier limits for flash-class models are a small number of requests per minute (I believe ~10 RPM / a few hundred per day for gemini-2.5-flash free tier - verify against current docs). `GeminiClient` has 2 attempts with 0.8-1.6 s sleep and no throttle/`retry-after`; 7 parallel calls will 429, fall back to rules, and the UI will show red errored nodes + fallback engine on most runs; a full `process_all` on Gemini would exhaust a daily quota. Model id `gemini-2.5-flash` and google-genai 2.18 call shape are unverified. If it works, it fixes the generalisation gaps above; if it does not, the demo still works on rules (good design).

### 6. Demo / pitch / UX

Strong: cinematic reference match, real 7 fields, engine badges, honest pills (REPLAY/LIVE), "Accuracy vs labels ... offline self-eval on the synthetic set" caption, keyboard shortcuts, 520-email list with filters/search, report table with evidence, mismatches highlighted, non-comparison classification card. LIVE and REPLAY both actually work. Defects I saw:
* Timeline step "Aggregate + compare" prints **"all 7 match"** for NEEDS_REVIEW emails where compare was skipped (email_512, 513, 517; unreadable/missing value). A false statement on the demo's key strip.
* At 1536x1024 in Report tab the Human-review block (the required HITL feature) is below the fold behind an inner scroll; buttons are not visible without scrolling.
* Default verdict for deterministic escalations = "escalation correct" -> the fly net update shown is a no-op ("0.63 -> 0.63 ... mean weight 1.00 -> 1.00 SIMULATED"), a judge clicking the obvious button sees nothing learn.
* "w 1.00 -> 1.00" chip overlaps the "suspicion / threshold" text in the fly panel after a review.
* Sidebar footer tagline "Smarter checks. Smoother trade." is copy from the reference art, meaningless for this product.
* Times such as 04:10/15:58 are simulated (`received_at` is not in the data) but displayed as if real; only the README says so.
* Human reviewer cannot see the source document, only 1-2 evidence lines; in the unreadable/scanned case there is nothing to review against, yet "Confirm: no mismatch" is accepted and flips status to OK.
* No way from the UI to demonstrate a processing failure/retry (API `inject_fail` only).
* No pitch script/deck material in the repo: no root README, no "what to click in 3 minutes", no slide with the honest fly story or the measured grey-zone table.

### 7. Rubric (each /10; weighted overall /100)

| Category (weight) | Score | Why |
|---|---|---|
| Accuracy (20) | 9 | 1.0000 verified, 520/520, 46/46; -1: vocab/allow-lists dataset-derived, false MISMATCH on mid-value truncation, ~25 formatting false alarms on unseen forms |
| Spec coverage (15) | 8 | all basics + PDF/Word/xlsx + HITL + retry API; scans only flagged, failures not demo-able from UI |
| Reliability / HITL (15) | 7 | deterministic triggers 20/20, live confirm/correct works, real gate grey-zone behaviour on my data; -3 for gate drops confirmed defects, mislabelled `missing_value`, uncertain classifier never asks for help, "all 7 match" lie, no source-doc view, replay HITL simulated |
| Architecture / innovation (15) | 8 | honest real graph, pure compare, evidence-verified Gemini design, interpretable few-shot gate that demonstrably learns locally (my curve); -2 gate invisible on shipped data, circular calibration, Gemini unverified |
| UX / demo (15) | 8 | best-in-class look, live+replay work, deployable; -2 for the misleading timeline text, below-fold review, no-op default verdict, tagline, no demo script |
| Robustness (10) | 5 | fail-safe by default (escalates rather than lies in most breakages) but brittle on layout/labels/legal forms/countries and undetected truncation |
| Docs / reproducibility (10) | 7 | backend + frontend READMEs accurate and candid, 62 tests, deterministic run; no root README/pitch, no innovator.md, scorer unicode crash undocumented, no pinned versions |
| **Overall** | **76 / 100** | (9x2 + 8x1.5 + 7x1.5 + 8x1.5 + 8x1.5 + 5x1 + 7x1) |

### 8. Critique by name

* DEVELOPER (backend): honest and unusually candid (no fake Gemini, engine labelled, tests 62, fixes at root not id special-cases, the 0.9606 -> 1.0 story is credible). Objections: (a) header comment/README claim "generic language" while `classifier.py` hard-codes this inbox's sender domains and subject templates and `normalize.py` a country list/UNLOCODE table copied from the data; the perturbation check in README (89 txt pairs re-rendered with MT/lbs) tested only unit/case/LTD-LIMITED, exactly the forms the author had already handled - not layout, labels, legal forms, countries, truncation. (b) gate-escalated results are labelled `missing_value`/`unreadable` and drop confirmed defects. (c) `many_mismatches` treated as anomaly conflicts with the e2e metric. (d) `GeminiClient` has no throttle for the very API the idea.md picked for its free tier. (e) 86 of 220 BL_COMPARISON emails ("send me the draft BL") are `OK` with `comparison_performed:false`; correct per the labels and honestly documented, but the sidebar "114 compared" vs classification 220 needs one sentence in the pitch. Credit: retry, review, replay export, no ground-truth leak in artefacts are all verified.
* DEVELOPER (frontend): adapters are correct against the real export (I verified) - the earlier "mock-verified only" gap is closed. Objections: timeline "all 7 match" text, review panel hidden below fold, simulated replay review with default no-op verdict.
* DESIGNER: layout fidelity is excellent and the logo/glow/fly-panel fixes are visible in my screenshots. Objections of DESIGNER (engine badges, small text floor) I agree remain open; add: the reference tagline placeholder, panel overlap in fly footer, and the Report tab spends 40% of height on the header/back row so the review block is pushed out of view. No design work addresses the review flow, which is the scored-by-spec feature.
* INNOVATOR: `reviews/innovator.md` does not exist (screenshots `innovator-*.png` do), so its claims cannot be checked against a log; the observable changes (replay Hebbian update with the real rule, help overlay, KC hover, sequence shots) are good. Objection: the interactive teaching demo only shows learning if the user picks "unneeded"; default path is a no-op. Write the log.

### 9. Ranked point-losers, with concrete fixes

1. **Gate invisible on real data + circular validation** (-4). Fix: generate the grey-zone replay set from `reviews/judge_scratch/perturb.py` mutations (typo consignee, conflicting weight line, truncated value, OCR read), export as `traces/grey_*.json` with "Grey-zone demo" filter chip, publish gate on/off table + 3-verdict curve; keep the 520-email score separate.
2. **Gate escalation drops confirmed defects and mislabels reason** (-3). Fix: keep a `confirmed_defect_fields` (state=mismatch and not near_miss) in the Result and in the submission when status stays MISMATCH; escalate to NEEDS_REVIEW only when the ambiguity is on a field that would decide the outcome; use a new UI reason "low confidence" and map it to `missing_value` only in `submission.json`; remove `many_mismatches` as auto-escalation or lower it to a review flag that leaves status MISMATCH.
3. **Rules brittleness / overfit vocabulary** (-3). Fix: (a) add separators `-`,`=`,`|`, 4-col and transposed docx/xlsx tables; (b) replace country list with "compare the part before the first comma/dash" + code-in-parens stripping + `pycountry`; legal forms `S/B`, `L.L.C.`, `GMBH & CO KG`, `THE`, `(S)`/`(M)` locale tags; (c) synonym table + generic fuzzy (token-set) for unseen labels e.g. `Gross Weight of Cargo`, `Consignee / Buyer`; (d) drop the sender allow-list, keep only content signals; (e) add `tests/test_messy.py` from `run_a..d.py` so the robustness is a regression suite.
4. **Undetected truncation -> false MISMATCH** (-2). Fix: flag a document whose last line lacks a newline/whose weight is < 1/10 of the other doc or that has < N of the standard SI/BL keys; escalate `unreadable`/`missing_value`.
5. **Scans not read** (-2). Fix: add `rapidocr-onnxruntime` (pure pip) or Gemini vision (`Part.from_bytes` PNG page) for image-only PDFs, set `ocr` flag (conf 0.6) so the gate escalates with the read text as evidence (keeps the label `unreadable`/NEEDS_REVIEW for the 3 scanned test emails, but the human sees the text - the natural gate showcase).
6. **Gemini path unverified / no throttle** (-2, high impact if judges use a key). Fix: one real smoke run before submission; `asyncio.Semaphore(4)`, exponential backoff honouring 429 retry-after, per-node timeout, cache by (doc hash, field); record measured quota in README.
7. **Timeline "all 7 match" on skipped compare** (-1.5). Fix: `Timeline.tsx` show the event `summary` / "not run" when compare is `skipped` or status NEEDS_REVIEW.
8. **HITL panel placement and default verdict** (-1.5). Fix: move "Human review needed" above the field table or make the Report tab header collapse; pre-select "unneeded" only for gate-decided escalations and require an explicit verdict click for deterministic ones; make the update visible (show the KCs changed).
9. **No pitch/README root + INNOVATOR log** (-1). Fix: root `README.md` (2 commands, modes, honest numbers, the fly story with measured grey-zone table, what is simulated), `reviews/innovator.md`, remove reference tagline, label simulated timestamps.
10. **Classifier uncertainty never asks for help; failure demo not reachable** (-1). Fix: if classifier confidence < 0.5 push into the same escalation UI ("classify as ... ?" human verdict); add a "Simulate node failure" toggle (calls `?inject_fail=`) in LIVE for the retry demo.

### 10. Blockers

None that stop submission: the 520-email output is correct, reproducible and well-shaped; REPLAY (Vercel) and LIVE both work. The items above are score/impression risks, not build breaks. Biggest risks for a judge session: (1) live Gemini attempt with a real key (untested, quota), (2) a judge feeding a hand-made messy document (layout/label/legal-form false alarms or silent mid-value truncation), (3) the "what does the fly do?" question with only deterministic triggers on screen.

STATUS: OBJECTIONS=10
1. Gate has no visible/measured role on shipped data; calibration is circular (ship a grey-zone replay set + gate on/off table).
2. Gate escalation drops confirmed defects and reports a wrong `review_reason` (`missing_value`); `many_mismatches` conflicts with the e2e metric.
3. Rules engine vocabulary/normalisation overfit and brittle (layouts, labels, legal forms, countries, classifier 78% on fresh phrasing).
4. Mid-value truncation gives a silent false MISMATCH (22/22 clean pairs).
5. Scanned PDFs are never read (no OCR/vision) - advanced item only partial.
6. Gemini path unverified live and unthrottled (429 -> red nodes/fallback for any judge with a key).
7. Timeline says "all 7 match" for skipped compare (false statement on NEEDS_REVIEW emails).
8. Human-review panel below the fold at 1536x1024; default verdict makes the Hebbian update a no-op; no source-document view; replay HITL simulated.
9. Docs/pitch: no root README, no `reviews/innovator.md`, reference tagline left in sidebar, simulated timestamps shown as real, no demo script.
10. Classifier uncertainty never escalates; processing-failure/retry cannot be demonstrated from the UI.

## Round 2 - JUDGE, 2026-09-22

Method: read ORCHESTRATION.md, CONTRACT.md (updated), idea.md, my own Round 1 (76/100, OBJ=10), developer.md Round 2, designer.md/innovator.md Round 1. Independently re-ran every claimed number from a clean checkout state (deleted `backend/out/flybrain_state.json` before the Hebbian check so "first click" numbers are not contaminated by my own testing). Did **not** reuse Round 1's harnesses for the adversarial re-tests DEVELOPER fixed against (`reviews/judge_scratch/run_a.py`, `unit_norm.py`, `cls.py`, `run_d.py` etc. stay untouched as-is) - wrote fresh ones instead: `fresh_countries.py`, `fresh_legal.py`, `cls_fresh2.py`, `run_fresh_layout.py`, `make_scan.py`/`make_scan2.py` + `run_fresh_ocr.py`/`run_fresh_ocr2.py` (two brand-new synthetic scanned PDF pairs, never seen by DEVELOPER), all in `reviews/judge_scratch/`. Drove LIVE (real `uvicorn`, ports freed after) and REPLAY (real `vite` dev server) in headless Chrome 153 over CDP (own script, `judge_scratch/cdp.py` - Claude-in-Chrome extension still not connected) at 1536x1024. Screenshots `reviews/shots/judge2-*.png`. All servers/Chrome verified stopped afterward (netstat clean on 5200/8000/8123/8124/9334).

### 1. Independent verification of DEVELOPER's numbers

| Check | Claimed | Verified | Match |
|---|---|---|---|
| `pytest -q` | 63 passed | **63 passed** (24.1s) | YES |
| `python -m sdoc.run_all` | 520 emails, ~33-37s | **520 emails, 32.3s** (454 OK/46 MISMATCH/20 NEEDS_REVIEW) | YES |
| `PYTHONUTF8=1 score_cli.py` | FINAL 1.0000 unchanged | **FINAL 1.0000** (s1=1.000, s3=1.000, e2e 46/46, escalation P=R=1.00, 5/5 per reason) | YES |
| `python -m tools.grey_zone_eval` | precision 1.00, recall 0.625, F1 0.769 | **precision 1.00, recall 0.625, F1 0.769** (tp=75,fp=0,tn=80,fn=45; buckets clean 0/40, clean_defect 0/40, near_miss_typo 25/40, conflicting_line 35/40, ambiguous_role 15/40) | YES, exact |
| submission `review_reason` boundary | `low_confidence` -> `missing_value` only at export | Direct call `submission_entry({...,"review_reason":"low_confidence",...})` -> `{"review_reason":"missing_value","has_defect":False,...}`; live `results.json` has 0 `low_confidence` on the real 520 (matches claim of "0 impact on real data") but 6 `missing_value`/4 `unreadable`/5/5 deterministic - matches submission.json exactly | YES |
| ground-truth leakage grep | none | `grep -rniE "ground_truth\|email_[0-9]{3}\|data_v2" backend/sdoc frontend/src` -> 0 hits | YES, still clean |
| `npm run build` | 0 TS errors | **0 TS errors**, 1899 modules, 323KB JS/47KB CSS | YES |
| `python -m sdoc.export_replay` | fresh replay after backend changes | Re-ran it myself: 520 emails, 6685 events, 5.0MB - frontend tested against this fresh export, not a stale one | YES |

**Timing note (not a bug):** `run_all` is now 32.3s vs Round 1's 17.8s (~1.8x). DEVELOPER attributes this to the added generalisation logic (OCR-capable paths, more synonym/layout matching) after fixing a genuine infinite loop they found and disclosed themselves in their own xlsx rewrite. Plausible and not something a judge would notice live (well under any reasonable demo/scoring timeout).

### 2. Fresh adversarial re-tests of my own Round 1 point-losers (new inputs, not reused)

**#4 Mid-value truncation (Round 1 -2, most severe silent-wrong bug) - CONFIRMED FIXED.** Re-ran my own Round-1 harness (`run_d.py cut_mid_weight`) unmodified: was 22/22 silent false MISMATCH, now 0/22 - all 22 correctly escalate `NEEDS_REVIEW`/`missing_value`. Verified in this session's output.

**#5 Scanned PDFs never read - CONFIRMED FIXED, with fresh (never-before-seen) scans, not the shipped `email_512-514`.** Built two brand-new synthetic image-only PDF pairs from scratch (different content, fonts, party names, ports - Malaysia/UAE and Sri Lanka/Colombia) and ran them through `docparse.parse_document` -> `extract_field` -> `compare_field` directly:
- Pair 1 (clean, all fields identical): OCR read both docs (`ocr_used=True`), extracted all 7 fields with realistic OCR noise ("JEBEL ALl" for "JEBEL ALI", "4x4o'HC" for "4 x 40'HC"), correctly failed safe to `NEEDS_REVIEW`/`missing_value` on the one field OCR mangled past parseability (container count) rather than guessing.
- Pair 2 (one real defect: container count 2 vs 3 x 20'GP): OCR read both docs and the pipeline correctly reported **MISMATCH, `defect_fields: ['container_count']`** - a real defect, in a real image-only PDF DEVELOPER never saw, correctly caught end-to-end. This is solid, generalised evidence for the OCR claim, not just "it works on the 3 known emails."

**#6 Gemini unverified/unthrottled - same status as DEVELOPER states.** `asyncio.Semaphore` + backoff-with-jitter code read and present; still genuinely unverified against a live key (none available here either) - DEVELOPER discloses this plainly rather than claiming it's done, so no objection on honesty, only the same residual risk as Round 1.

**#7 Timeline "all 7 match" on skipped compare - CONFIRMED FIXED, live in the browser.** Selected `email_511` (deterministic `unreadable` trigger, compare skipped) in REPLAY: timeline now reads **"not run"** for Aggregate + compare (screenshot `judge2-511-report.png`), not the old false "all 7 match". Code fix (`Timeline.tsx` checking `compare?.state === 'skipped'`) matches.

**#8 HITL panel below the fold / no-op default verdict - CONFIRMED FIXED, live in the browser and re-derived math.**
- Panel position: screenshot `judge2-review-513.png` (1536x1024, REPLAY, `email_513`) shows "Human review needed" with all three action buttons (Confirm mismatch / Confirm: no mismatch / Correct a field) fully visible immediately below the headline, well above the fold - no scrolling needed.
- No-op fix: deleted `flybrain_state.json` for a truly clean state, ran `email_513` through a real LIVE `uvicorn` backend, called `POST /api/review` with the default-selected verdict ("escalation correct"). Per-KC weights: **0.9 -> 0.95** for all 80 active KCs (real numpy output, not rounded-away) - a genuine, visible change, not the old `w=1.0` ceiling no-op. In the browser (REPLAY, same email after a fresh session) the UI showed "mean weight 0.81 -> 0.91" and a live per-KC ring/label animation. Confirmed deterministic-trigger escalations correctly report `{skipped:true, reason:"...deterministic trigger..."}` and do **not** call `gate.learn()` (verified via `email_511` review call) - the two-part root cause DEVELOPER diagnosed is real and both halves are fixed.
- Residual, correctly-scoped nuance: on a **second** consecutive `escalation_correct` review touching overlapping Kenyon cells, the aggregate suspicion number can already be near the 1.0 ceiling and show no further movement - this is the LTP formula's own bounded-saturation behaviour (mathematically expected, not a bug), and the per-KC delta (the number the UI actually displays) is still real that call too, just may itself be small once weights approach 1.0. Not a regression; noted for completeness.
- Not claimed by DEVELOPER and still absent: no source-document viewer (human still sees only 1-2 evidence lines, same as Round 1) - a real, still-open UX gap, but DEVELOPER never claimed to have fixed it, so this isn't a false claim, just an unresolved item from Round 1 #8 carried forward.

**#10 Classifier low-confidence escalation / failure-retry not reachable from UI - CONFIRMED FIXED (code path + live UI).** Read `pipeline.py`'s non-comparison branch: every category (not just BL_COMPARISON) now routes classifier confidence through `build_input_vector`/`gate.decide`, and a genuinely-uncertain classification can independently trigger `NEEDS_REVIEW`/`low_confidence` even with a confident category match elsewhere - this can only add escalations, never suppress one, and the dedicated test (`test_low_confidence_non_comparison_can_escalate`) exists and passes. Live UI: clicked the real **"Simulate failure"** button (LIVE mode only, next to Re-run) on `email_004` - produced a red Classifier node, red Report node, "A node failed. Retry" banner, all downstream nodes greyed to "waiting" (screenshot `judge2-live-simfail.png`); clicking Re-run recovered the pipeline fully back to the correct MISMATCH/2-defects result (screenshot `judge2-live-rerun-after-fail.png`). This is a real, demoable, end-to-end fix of the Round 1 complaint that failure could only be produced via a raw query string.

**#2 Gate drops confirmed defects / mislabels `review_reason` - CONFIRMED FIXED, live.** Ran `email_513` (a real scanned email, gate-decided `flynet` escalation) through a fresh LIVE backend: `has_defect: true, defect_fields: ["shipper"]` are **kept** on the `NEEDS_REVIEW` Result (not wiped), and `review_reason` is `"unreadable"` (correctly kept honest for an OCR-flagged doc, not silently mapped to `low_confidence` - that mapping only applies when nothing is missing/unreadable at all, a real design nuance I verified in `pipeline.py` line 275, not a bug). `many_mismatches` fix verified by reading `flybrain.py build_input_vector`: it only fires as an anomaly signal when at least one of the >=3 mismatches is itself shaky (near-miss or low-confidence) - a clean, confident 3-field defect no longer gets gate-escalated away from its correct MISMATCH status (confirmed: `clean_defect` bucket in grey-zone eval = 0/40 false escalations).

**#1 Gate inert on real data / circular calibration - CONFIRMED FIXED as a genuine capability**, not just a claim: the `grey_zone_eval.py` numbers reproduce exactly (see table above), the buckets and method (real bundle SI/BL text, hand-mutated, run through the actual pipeline - not the old synthetic 32-d vectors) are legitimately non-circular, and I additionally confirmed `email_513`/`514` (real, not synthetic) escalate with `decided_by: "flynet"` on the live 520-email run, not a deterministic rule.

### 3. New findings from genuinely fresh inputs (not reused, not seen by DEVELOPER)

**A. [Medium] Legal-suffix normalisation still produces a silent, high-confidence false MISMATCH for extremely common global suffix punctuation - same bug class as the (now-fixed) truncation bug, just a different trigger.** DEVELOPER's fix explicitly targeted `S/B`, `L.L.C.`/`FZ-LLC`, `GMBH & CO KG`, leading `PT`/`THE`, and specific parenthetical locale tags (`(S)`, `(M)`) - all verified working. But dotted 2-3-letter suffixes outside that list are **not** collapsed before the generic non-alphanumeric strip, so e.g. `"IBERIA TRADE S.A."` tokenises to `S A` (two stray letters) while `"IBERIA TRADE SA"` correctly strips the recognised `SA` suffix token - the two never converge. Reproduced directly against `sdoc.normalize.names_equal` and the full `sdoc.compare.compare_field`/`decide()` pipeline with fresh names (none copied from `unit_norm.py`):
```
names_equal("IBERIA TRADE S.A.", "IBERIA TRADE SA")     -> match=False, near_miss=False   (BAD - same company)
names_equal("DELTA HOLDINGS N.V.", "DELTA HOLDINGS NV") -> match=False, near_miss=False   (BAD)
compare_field('shipper', ...) on the pair above -> {'match': False, 'state': 'mismatch', 'confidence': 0.95, 'near_miss': False, 'note': None}
```
Fed through the full 7-field `decide()` with an otherwise-identical SI/BL pair (S.A./B.V. suffixes on shipper/consignee/notify_party, everything else matching): **`MISMATCH, has_defect=True, defect_fields=['shipper','consignee','notify_party']`** - a completely clean pair reported as a 3-field defect, at 0.95 confidence, `near_miss=False` so it never even reaches the fly gate's grey-zone path. 9 of 17 fresh legal-suffix pairs I tried failed this way (`A.G.`, `S.A.`, `S.A. DE C.V.`, `N.V.`, `B.V.`, `SP. Z O.O.`, `K.K.`, `D.O.O.`, `S.A.R.L.` vs their undotted forms); the forms DEVELOPER did generalise (`LDA`, `LTDA`, trailing-space, plus the ones already verified in finding D below) passed. **Does not affect the shipped 520-email score** - grepped the actual dataset, 0 occurrences of any of these suffixes in `work/bundle/attachments/*.txt`. This is a concrete, reproducible instance of Round 1 objection #3 ("rules brittleness... legal forms") that DEVELOPER's writeup calls "FIXED (broadly)" - broadly is accurate, but the specific gap is a real, silent, non-fail-safe false positive on suffix forms that are arguably *more* common globally (S.A./N.V./B.V. cover most of Latin America and continental Europe) than the ones that were fixed.

**B. [Medium] Classifier generalisation to genuinely fresh phrasing is still weak; DEVELOPER's "23/23" measures fit to a known probe, not to new language.** Wrote 18 new BL_COMPARISON/SI_REQUEST/INVOICE_QUERY/GENERAL/SPAM emails in different wording than Round 1's `cls.py` (which DEVELOPER fixed against directly, reaching 23/23 on *that* file - reproduced, still 23/23). My fresh set: **11/18 correct (61%)**, worse than even my own Round 1 fresh-phrasing number (78%). Two concerning failure modes, both consequences of a heuristic DEVELOPER explicitly flagged as declined-with-reason (developer.md #3a, "sender-domain-vs-TLD nudge... declined for this round, logged here"):
- A genuine 2-attachment BL_COMPARISON request from `cs@forwarder.net` ("Please double check before we release the B/L") was classified **SPAM (0.47)** purely because `.net` matches the hard-coded spam-TLD regex - meaning a real document-comparison email would never even reach extraction.
- `export.team@factory.co` (SI_REQUEST wording) -> SPAM 0.67 (`.co` TLD); `finance@client.org` (INVOICE_QUERY wording, "Discrepancy on the latest bill") -> SPAM 0.67 (`.org` TLD). `.co`/`.org`/`.net` are mainstream business TLDs, not spam signals.
- Separately, a real phishing pattern ("mailbox quota exceeded... verify your credentials") was missed entirely (classified GENERAL) since it doesn't match the specific prize/lottery/CEO-fraud wording DEVELOPER added.
DEVELOPER already disclosed the TLD heuristic as a known, deliberately-undone risk rather than claiming it fixed - so this isn't a false claim, but my fresh test gives it concrete teeth: it isn't a hypothetical, it demonstrably drops a real comparison request into SPAM and inflates SPAM false positives on ordinary corporate senders. Recommend actually removing or heavily down-weighting the TLD nudge before a live demo where a judge might type a fresh email.

**C. [Low] Layout coverage widened but still narrow; some forms fail completely, not just partially.** Fresh separators not in `run_a.py` (`-->`, `;`, `>`, parenthetical-value, 4-space indentation, numbered-list prefix) against the real pipeline (`run_fresh_layout.py`, 84 real BL_COMPARISON pairs per case): `::` and pure-JSON-ish `"Label": "value"`, and double-space-then-colon all still parse correctly (0/84 changed - the underlying colon-based regex is more forgiving than I expected). But arrow/semicolon/`>`/parenthetical-value fail exactly like the pre-fix dash/equals did (72/84 -> fail-safe `NEEDS_REVIEW`/missing_value, not silent) - and **indentation and numbered-list prefixes break every single field on every email in the bucket (84/84, all 7 fields "missing")**, worse than a partial miss. Still fail-safe (never a false MISMATCH), so this is a "brittle" finding not a "wrong answer" finding, but it shows "Layouts: FIXED (broadly)" added exactly the 3 separators tested against and nothing beyond that specific list.

**D. [Info, low] Minor spelling-variant miss, but the country-list fix itself is confirmed solid.** `"BASRA, IRAQ"` vs `"BASRAH"` (an alternate transliteration of the same port) doesn't match - separate from, and much smaller than, the country-list fix, which I verified genuinely generalises: 15/16 fresh Iran/Iraq/Colombia/Argentina/Ecuador port pairs (different ports and phrasing than `unit_norm.py`'s list, including official long-form names like "Islamic Republic of Iran" and "Republic of Iraq") matched correctly, and all 6 negative pairs (different countries, including an Iran-vs-Iraq near-miss) correctly did NOT match. This specific fix (`pycountry` full country database) is confirmed to genuinely generalise beyond the 5 named countries, not just cover them by name.

**E. [Cosmetic, non-blocking] Transient miscategorisation during a live simulated failure.** While the classifier node is red (mid-failure, before Retry/Re-run), the email-list row briefly shows category "General" and drops its defect chip for an email that is actually BL_COMPARISON/MISMATCH - resolves correctly the instant the pipeline is re-run/retried, and arguably makes sense (there genuinely is no valid classification while that node is broken), but could read as a wrong number if a judge freezes on that exact frame during a live demo. Not worth a fix given the deadline; flagging for awareness only.

### 4. Frontend/backend re-verification matrix (visual, live)

| Item | Status | Evidence |
|---|---|---|
| REPLAY drives real (re-exported) 520-trace data | YES | `judge2-replay-idle.png`; fly panel shows "1600 of 1600 Kenyon cells drawn" |
| Report view, clean MISMATCH email | Correct | `judge2-report-email004c.png`: 2 real mismatches, gate correctly stays confident (suspicion 0.00, not escalated) even though 2 fields differ - the `clean_defect`-style behaviour holds on a real dataset email, not just the eval script |
| Timeline "not run" fix | CONFIRMED live | `judge2-511-report.png` |
| HITL panel above the fold | CONFIRMED live | `judge2-review-513.png` |
| Hebbian update non-trivial + honest skip on deterministic triggers | CONFIRMED live (fresh state + REPLAY) | `judge2-review-513-after.png` ("mean weight 0.81 -> 0.91"); direct API calls on a clean state (0.9 -> 0.95 per-KC) |
| LIVE mode, Simulate failure -> red nodes -> Re-run recovers | CONFIRMED live | `judge2-live-simfail.png`, `judge2-live-rerun-after-fail.png` |
| Root README | Read in full | Honest, accurate against reproduced numbers, includes 3-minute demo script and a candid "known limitations" section (correctly still lists Gemini-unverified and `ambiguous_role` recall as the weakest measured category) |
| `npm run build` | 0 errors | Confirmed |

### 5. Updated rubric (same categories/weights as Round 1)

| Category (weight) | R1 | R2 | Delta | Why |
|---|---|---|---|---|
| Accuracy (20) | 9 | 9 | 0 | 1.0000 re-verified exactly; truncation false-MISMATCH (R1 reason) is fixed, but a same-severity new class (S.A./N.V./B.V. suffix false MISMATCH, finding A) re-earns the same deduction under a different trigger - net unchanged |
| Spec coverage (15) | 8 | 9 | +1 | OCR now actually reads scans (verified on 2 brand-new fabricated pairs, one with a real caught defect) instead of only flagging them; classifier-uncertainty escalation and failure/retry UI close the remaining spec gaps |
| Reliability / HITL (15) | 7 | 9 | +2 | Defects kept on gate escalation, honest `review_reason`, non-circular grey-zone measurement, panel above fold, real non-trivial Hebbian update with correct deterministic/flynet separation, live Simulate-failure UI - essentially all of Round 1's reliability objections independently re-verified fixed |
| Architecture / innovation (15) | 8 | 9 | +1 | Gate's grey-zone job is now genuinely measured (non-circular), OCR is real, Gemini throttling code exists (still unverified live, same residual as R1) |
| UX / demo (15) | 8 | 9 | +1 | Timeline lie fixed, HITL panel fixed, no-op verdict fixed, tagline fixed, root README + demo script added, failure now demoable from the UI; source-document view still absent (unclaimed, carried over) |
| Robustness (10) | 5 | 7 | +2 | Truncation (the worst bug) and OCR both closed and independently re-verified; but fresh testing found a same-class silent false-MISMATCH bug (legal suffixes) and materially weaker-than-claimed classifier generalisation (61% on fresh phrasing, plus a real comparison-request-into-SPAM repro) - real progress, not yet "robust" |
| Docs / reproducibility (10) | 7 | 9 | +2 | Root README (honest, demo script, known limitations), grey-zone eval documented and reproduced exactly, all numbers in this round matched what I independently ran |
| **Overall** | **76** | **88** | **+12** | 9x2 + 9x1.5 + 9x1.5 + 9x1.5 + 9x1.5 + 7x1 + 9x1 |

### 6. Critique by name

**DEVELOPER** - the strongest Round 2 I've reviewed on this project. Every one of my 10 Round 1 objections got a real, independently-reproducible fix or an honestly-disclosed partial (Gemini live verification, correctly still blocked on "no key available" rather than faked). The write-up is unusually candid about what was *not* done and why (declined the sender-TLD removal with a stated regression-risk reason, declined porting my harnesses verbatim into `tests/` with a stated reason, disclosed and fixed their own infinite-loop bug mid-round before it shipped). That candour is exactly why finding A and B above land as "confirms a declared residual risk with concrete evidence" rather than "caught a hidden false claim" - I want to be precise about that distinction because it matters: DEVELOPER did not claim the legal-suffix or classifier-generalisation problems were solved, and my fresh tests show they are real but were already flagged in some form. The one place I'd push back: "Layouts: ...FIXED (broadly)" and "Rules engine overfit/brittle - FIXED (broadly)" are headline-labelled as resolved items in a `STATUS: NO REMAINING OBJECTIONS` round; "broadly" is doing a lot of work in both cases (3 separators added out of many possible; the specific suffix forms in the fix list, not the category). A more conservative status line ("FIXED for the forms tested; broader generalisation remains open") would have matched the actual scope more precisely - this is a documentation-precision note, not a code objection.

**DESIGNER / INNOVATOR** - not rerun this round per ORCHESTRATION's plan (their own Round 2 comes after this). I re-verified the two items DEVELOPER claims to have handled on their behalf: designer's engine-badge logic (`GraphPanel.tsx runDefaultEngine`/`OddTag`) is untouched and still present in the built app; the tablet dead-space decline is a reasonable use of the "explicit permission to decline a non-cheap fix outside the primary target" rule and I don't see a cheap fix either. Innovator's two requests (`eta_dep`/`eta_pot`/`tau`/`init_w` in `flybrain.json`, and `kc_active`/`kc_weights_before`/`kc_weights_after` directly in `POST /api/review`'s response) are both verified present in the real exported `flybrain.json` and in a real live API response - DEVELOPER's claims on their behalf check out exactly.

### 7. Submit-now readiness (today, 22 Sep 12:00 PM)

**No blockers.** The 520-email score (1.0000) is correct, reproducible, and I independently reproduced every number DEVELOPER reported this round, including the new non-circular grey-zone metric. REPLAY (the Vercel/no-backend path) and LIVE both work end-to-end, including the failure/retry demo path that was missing in Round 1. Everything in section 3 above is a genuine but bounded generalisation gap (none of it touches the shipped 520-email dataset - I grepped to confirm the legal-suffix gap has zero occurrences in the real data) or an already-disclosed residual (Gemini-live, sender-TLD heuristic). If there's spare time before 12:00 PM and a priority order is wanted: (1) finding A (legal-suffix dotted-abbreviation generalisation - same one-line pattern as the existing `_LEGAL_ABBREV` list, low risk, closes the last "silent wrong answer" class I could find), (2) toning down or removing the sender-TLD spam nudge (finding B) since it can drop a real comparison request into SPAM, ahead of anything else - the rest is polish. If a judge session runs out of time before either, ship as-is; nothing here is worse than what was already disclosed in `backend/README.md`'s "known limitations".

STATUS: OBJECTIONS=3
1. [Medium] Legal-suffix normalisation still gives a silent, high-confidence false MISMATCH for common global dotted suffixes (S.A., N.V., B.V., A.G., K.K., D.O.O., S.A.R.L.) not covered by this round's fix list - verified end-to-end through `compare.decide()`, not just the normaliser. Zero occurrences in the shipped 520-email dataset (score unaffected); real risk on any actual European/Latin American shipping inbox.
2. [Medium] Classifier generalisation to fresh phrasing remains weak (61% on 18 new emails I wrote, none copied from Round 1's probe) and the already-disclosed sender-TLD heuristic demonstrably drops a real 2-attachment BL_COMPARISON request into SPAM and false-flags ordinary `.co`/`.org`/`.net` senders as SPAM - concrete evidence behind a risk DEVELOPER already flagged as declined, not a new hidden defect.
3. [Low] Layout coverage was widened by exactly the 3 separators tested against (dash/equals/markdown) and remains fail-safe-but-brittle on others (arrow, semicolon, indentation, numbered lists); indentation alone breaks all 7 fields on every email in the bucket. Fail-safe (never a silent wrong answer), so lower severity than #1.

### 8. Addendum: concurrent DESIGNER Round 2 landed mid-session (self-correction)

While finishing this review I found `reviews/designer.md` had gained a new `## Round 2` section and an auto-commit (`83fc94d`, "designer round 2 - tablet layout crash, honest hebbian skip message") had already landed, apparently dispatched by the orchestrator in parallel with this JUDGE Round 2 pass. Two real things it fixed that I should be transparent about: (a) `FlyPanel.tsx` was, until that commit, unconditionally rendering the generic "Weights updated ... suspicion X -> X" line even for a deterministic-trigger review whose backend response correctly said `{skipped:true,...}` - i.e. the exact "misleading no-op line" DEVELOPER's item #8 claimed to have eliminated was still visible in the one component a judge would actually be looking at, because the frontend never checked `feedback.skippedReason`. I did not catch this myself: my own verification of the deterministic-trigger skip used a raw `curl` to `/api/review` (confirmed the backend correctly returns `{skipped:true}`) plus a click-through of the browser UI only for a **flynet-decided** email (`email_513`); I never clicked through a deterministic-trigger review in the running UI, which is exactly where this bug lived. DESIGNER caught it by doing that exact click-through. (b) a genuine tablet-width (`<=1180px`) rendering bug - not "dead space" as Round 1 called it, but the whole graph rendered ~14,000px off-screen with an unbounded grid track - also got fixed in the same commit. Both fixes are small, targeted CSS/one-branch changes; I read the diff (`git show 83fc94d -- frontend/src/components/FlyPanel.tsx frontend/src/styles.css`) and it is correct and low-risk. I have not re-run my own browser verification against this newest commit (out of scope for a DEVELOPER-focused round under today's deadline), so I'm not changing my score for it, but flagging it for the orchestrator: (1) DESIGNER Round 2 has already happened and reported `STATUS: NO REMAINING OBJECTIONS`, so the orchestrator does not need to re-dispatch it; (2) this is a good example of why "I tested the backend flag" is not the same as "I clicked through the UI for that exact case" - worth keeping in mind for INNOVATOR's upcoming Round 2 verification too.

## Round 3 - JUDGE, 2026-09-22 (deadline day, likely final round)

Method: read ORCHESTRATION.md, my own Round 1/2 (76 -> 88, OBJ 10 -> 3), and `developer.md` Round 3 in full (fixes to all 3 Round 2 objections + one self-disclosed residual: classifier true-holdout 73.3%). Independently re-ran every claimed number from this session's own shell (no reuse of DEVELOPER's numbers). Wrote fresh, never-before-used test material for the two substantive claims (legal suffixes, classifier) rather than trusting the disclosed figures at face value. Scope check: `git log`/`git diff --stat HEAD~1 HEAD` confirms exactly the 3 files DEVELOPER named changed (`classifier.py`, `normalize.py`, `rules_extract.py`, 99 insertions/12 deletions) - no frontend, no test, no data file touched. Read the full diffs of all 3 files line by line, not just the summary.

### 1. Regression / headline numbers - all reproduced exactly

| Check | Claimed | Verified | Match |
|---|---|---|---|
| `pytest -q` | 63 passed | **63 passed** (24.6s) | YES |
| `python -m sdoc.run_all` | 520 emails, 454/46/20 unchanged | **520 emails, 34.3s, 454 OK / 46 MISMATCH / 20 NEEDS_REVIEW** | YES |
| `PYTHONUTF8=1 score_cli.py` | FINAL 1.0000 unchanged | **FINAL 1.0000** (s1=1.000, s3=1.000, e2e 46/46, escalation P=R=1.00, all 4 reasons 5/5) | YES |
| `python -m tools.grey_zone_eval` | bit-identical to Round 2 | **precision 1.00, recall 0.625, F1 0.769**, tp=75/fp=0/tn=80/fn=45, same per-bucket breakdown (clean 0/40, clean_defect 0/40, near_miss_typo 25/40, conflicting_line 35/40, ambiguous_role 15/40) | YES, exact |
| ground-truth leakage grep | none | `grep -rniE "ground_truth\|email_[0-9]{3}\|data_v2" backend/sdoc frontend/src` -> 0 hits | YES, still clean |
| `npm run build` | 0 TS errors, no frontend change | **0 TS errors**, 1899 modules, 323KB JS/47KB CSS; `git diff --stat` confirms 0 frontend files touched this round, so this is correctly a no-op check | YES |
| my own Round 1/2 harnesses re-run unmodified | no change | `run_a.py` 0/84 changed x10 layouts; `unit_norm.py` 125/138 (identical); `fresh_countries.py` 21/22 (identical); `cls.py` 23/23 (identical) | YES, no regression anywhere |

### 2. Legal-suffix fix - re-verified, then independently stress-tested with 13 brand-new jurisdictions/formats DEVELOPER never wrote

`reviews/judge_scratch/fresh_legal.py` (my own Round 2 harness) re-run unmodified: **17/17**, matches DEVELOPER's claim exactly.

Read `backend/sdoc/normalize.py`'s diff in full: the fix is a genuine root-cause generalization, not a patch list - `_DOTTED_INITIALISM = re.compile(r"\b[A-Z](?:\.[A-Z]){1,4}\.?\b")` collapses *any* 2-5-letter dotted initialism before the generic strip runs (same mechanism class as the pre-existing LLC/S-B collapses), plus `_LEGAL` genuinely broadened (Scandinavia, Iberia/LatAm, Japan, Balkans, CEE/Baltics, US professional forms) rather than the 7 specific items I'd reported failing.

I then wrote 13 completely fresh cases (`judge_r3_legal.py`, scratchpad) spanning jurisdictions/formats absent from both `fresh_legal.py` and DEVELOPER's own new-case list: Brazilian `S/A`, Indian `Pvt. Ltd.`, South African `(Pty) Ltd`, Russian `O.O.O.`, Hong Kong `Co., Limited`, Swiss accented `Sàrl`, Turkish `A.S.`, Singaporean `PTE. LTD.`, German `GMBH & CO. KG` with `&`-vs-`AND`, Malaysian `SDN. BHD.` with trailing dots, plus 3 negative controls (different core name, different suffix, typo).

**12/13 correct**, including every genuinely novel jurisdiction I could think of - strong evidence the fix generalizes rather than pattern-matching the disclosed list. One narrow gap found: `"BRASILIA EXPORT S/A"` vs `"BRASILIA EXPORT SA"` does not match (`names_equal` returns `match=False, near_miss=True`) - the `S/A` slash-abbreviation form (Brazilian/Portuguese "Sociedade Anônima") isn't covered by the existing `S/B` special case. Traced through the full pipeline (`compare_field` with realistic `Extracted`-shaped dicts, not raw strings): this produces `state='mismatch', confidence=0.9, near_miss=True` - materially **less severe** than the pre-fix S.A./N.V./B.V. bug (which was `near_miss=False`, i.e. zero gate protection). Because `near_miss=True` feeds the fly gate as a genuine grey-zone signal, `grey_zone_eval`'s own measured `near_miss_typo` recall (0.625) says this specific pair has roughly a 5-in-8 chance of being escalated to human review rather than silently reported as a confident defect - not perfect, but a materially different (mitigated) risk class from the bug this round fixed. Confirmed **zero occurrences** of `S/A` in the real dataset (`grep -rlE "S ?/ ?A\b" work/bundle/attachments/*.txt` -> 0 hits), so the shipped score is unaffected. Given the proportionality bar for this round, I'm not opening this as a numbered objection - it's the same long-tail shape as issues already accepted-as-disclosed in Round 2 (there will always be one more slash-form abbreviation for a regex-based name normalizer) - but it's worth a one-line note in a future pass if there ever is one.

### 3. Classifier fix - root-cause verified via code read; my own fresh 18-email batch (new domains, new phrasing, spammy-TLD+attachments cases) corroborates DEVELOPER's disclosed ceiling rather than contradicting it

`reviews/judge_scratch/cls_fresh2.py` (my own Round 2 fresh-18 probe) re-run unmodified: **18/18**, matches DEVELOPER's claim (was 11/18). `cls.py` (Round 1 probe) unchanged **23/23** - no regression on the existing probes.

Read the `classifier.py` diff in full: the TLD-nudge fix is structural, not a special case for `cs@forwarder.net` - it computes `non_spam_signal` (strongest non-SPAM content score already found) and `has_attachments` **before** applying any TLD nudge, and skips the nudge entirely when either is present; only a genuinely bare, unattributable message still gets the full +3. This is exactly the kind of root-cause fix that should generalize to senders/domains never used to write it - so I tested that claim directly rather than trusting it.

Wrote my own fresh 18-email batch (`judge_r3_cls.py`, scratchpad): new sender domains never appearing anywhere in this project's prior rounds or DEVELOPER's write-up (`oceanline-agents.net`, `westbridge-cargo.biz`, `harbor-trade.info`, `brightfield-imports.com`, `meridiantrade.org`, `vantagepoint.co`, `pacificgateway-forwarding.net`, `newshipperco.biz`, `luckywinner.click`, `account-alert-team.top`, `urgentrequest.xyz`), new phrasing throughout, and specifically **3 BL_COMPARISON-with-real-attachments emails from spammy TLDs** (`.net` x2, one deliberately styled like the originally-bugged case: "Please compare before release" from `review@pacificgateway-forwarding.net` with 2 PDF attachments) - the exact bug class Round 2 found.

Result: **14/18 (77.8%)**. Critically, **all 3 of my fresh spammy-TLD-plus-attachments BL_COMPARISON cases classified correctly** (none fell into SPAM) - the specific required bug does not reproduce on inputs invented independently of the fix. The 4 misses were all no-attachment, weak/off-pattern-content emails on spammy TLDs (`billing@meridiantrade.org` "Totals on this statement look off" -> SPAM; two GENERAL out-of-office/thank-you notes from `.biz`/`.info` senders -> SPAM; `firsttime@newshipperco.biz` "Our first booking - unsure what's required" -> SPAM) - exactly the failure shape DEVELOPER's own true-holdout write-up describes ("all 4 misses are senders with no attachments and phrasing outside any added pattern"). My independent number (77.8%) is close to and slightly better than DEVELOPER's disclosed 73.3% true-holdout figure, using a batch I wrote without ever seeing DEVELOPER's holdout set (which isn't persisted in the repo - confirmed via `find . -iname "*holdout*"`, no hits) - this corroborates the disclosure as an honest, representative number rather than a cherry-picked one.

### 4. Layout fix - re-verified, and the parenthetical-value revert's stated reasoning independently confirmed against the real dataset

`reviews/judge_scratch/run_fresh_layout.py` re-run unmodified: **5/6 layouts now 0/84 changed** (arrow, semicolon, `>`, numbered-list, indented - matches DEVELOPER's claim exactly, including catching that indentation was previously breaking ALL 7 fields on every email, worse than a partial miss). Parenthetical-value: **72/84 changed, all to `NEEDS_REVIEW`/`missing_value`** - fail-safe, not a false MISMATCH, confirming the revert left the pre-existing (already-known, already-accepted) fail-safe gap rather than a regression.

Read the `rules_extract.py` diff line by line: the `{2}` -> `{2,}` continuation-line-guard fix (`_scan`'s indentation check) is a real, previously-undiscovered off-by-logic bug, not cosmetic - confirmed by inspection that `^\s{2}[A-Za-z]` only matches *exactly* 2 leading spaces, so any document indented 4+ spaces (a real, common layout) had every line rejected as a spurious "continuation line". The parenthetical-value attempt is documented as a code comment explaining the revert, not left as dead/broken code - clean.

Independently verified DEVELOPER's stated reason for reverting (real values in this dataset already contain legitimate parens, so a second trailing-paren grab garbles them into a false answer rather than failing safe): `grep -rlE "\([A-Z]{2,}\)|\(Non-Negotiable\)" work/bundle/attachments/*.txt` -> **187 files** hit, and a sample of the actual matched text confirms real, legitimate parenthetical content (UN/LOCODE qualifiers like `AUSTRALIA (AUBNE)`, `CHINA (CNSHA)`, name/party qualifiers like `APRIL FAR EAST (M)`, unit-in-parens headers like `Gross Wt (kg)`). This reasoning is sound and dataset-grounded, not a convenient excuse - reverting was the right call.

### 5. Verdict on the disclosed residual (classifier 73.3% true-holdout ceiling)

**Acceptable as disclosed; not a blocking objection.** Reasoning:
- The *specific required bug* (spammy-TLD sender outranking a real attachment-bearing document-comparison request) is fixed at the root and demonstrably generalizes - I could not reproduce it on 3 fresh sender/domain/phrasing combinations invented specifically to try.
- The residual (open natural-language phrasing, no attachments, falls through to the TLD-driven SPAM default) is an inherent property of a keyword/regex classifier attempting open-domain intent classification without an LLM - not a hidden defect, not something a few more regex lines will structurally close (Round 1 and Round 2 both already showed that pattern: broadening against one fresh set regresses against the next).
- DEVELOPER reported the true number plainly (73.3%) rather than the tuned-set number (100%), and explicitly labelled it "not claimed as solved" - this is exactly the honest-disclosure behavior the brief asks a judge to reward, not penalize.
- My own independently-written fresh batch (77.8%, from senders/phrasing DEVELOPER never saw) corroborates the disclosed figure rather than contradicting it - both numbers describe the same real, bounded ceiling, not a false claim.
- Zero impact on the shipped, scored 520-email dataset: `run_all`/`score_cli` reproduced at exactly 1.0000 with 454/46/20, unchanged.
- The idea.md architecture itself anticipates this: rules-only is the fallback path when no `GEMINI_API_KEY` is present (none is, on this machine, per ORCHESTRATION.md), and the Gemini path exists in code for exactly this generalization gap - it's just unverified live (a residual carried since Round 1, already accepted, not new).

Given today's deadline and that DESIGNER/INNOVATOR have already both closed with `NO REMAINING OBJECTIONS`, dispatching a Round 4 to chase a rules-engine's open-natural-language ceiling further would trade real submission risk for diminishing, whack-a-mole returns on a limitation that is already honestly documented in `backend/README.md`'s "known limitations" section (verified present).

### 6. Updated rubric

| Category (weight) | R1 | R2 | R3 | Delta | Why |
|---|---|---|---|---|---|
| Accuracy (20) | 9 | 9 | **10** | +1 | The S.A./N.V./B.V. legal-suffix false-MISMATCH class (the reason Accuracy stayed at 9 in R2 despite 1.0000) is now fixed via a genuine root-cause regex, independently re-verified across 12 brand-new jurisdictions I invented myself (Brazil, India, South Africa, Russia, HK, Switzerland, Turkey, Singapore, Germany, Malaysia). The one narrow gap I found (`S/A`) is a materially lower-severity, gate-protected (`near_miss=True`), zero-real-data-occurrence edge case, not a repeat of the same silent-wrong-answer class - 1.0000 re-verified exactly |
| Spec coverage (15) | 8 | 9 | 9 | 0 | Unaffected by this round's changes (no OCR/HITL/spec-surface work this round) |
| Reliability / HITL (15) | 7 | 9 | 9 | 0 | Unaffected by this round's changes (gate/HITL code untouched; `grey_zone_eval` bit-identical, confirmed) |
| Architecture / innovation (15) | 8 | 9 | 9 | 0 | Unaffected; same residual as R1/R2 (Gemini path still unverified live, same disclosed risk, not worsened) |
| UX / demo (15) | 8 | 9 | 9 | 0 | No frontend changes this round (verified via `git diff --stat` and `npm run build`) |
| Robustness (10) | 5 | 7 | **9** | +2 | The two live robustness gaps from R2 (legal-suffix false-MISMATCH, 61% fresh-phrasing classifier with a real SPAM-misroute repro) are both substantively fixed and independently re-verified generalizing to material I wrote myself, not just DEVELOPER's own fresh set; layout coverage widened correctly with a real regex bug caught and fixed, and the one attempted-but-unsafe layout was honestly reverted rather than shipped broken. Not a 10: classifier still has a real, disclosed ceiling on genuinely novel no-attachment phrasing (~73-78% on independent fresh batches) |
| Docs / reproducibility (10) | 7 | 9 | 9 | 0 | Same high bar maintained - DEVELOPER disclosed and fixed an unrelated pre-existing-state pytest flakiness (stale local `flybrain_state.json`) mid-round rather than silently working around it, and reported the true-holdout classifier number plainly instead of only the tuned number |
| **Overall** | **76** | **88** | **92** | **+4** | 10x2 + 9x1.5 + 9x1.5 + 9x1.5 + 9x1.5 + 9x1 + 9x1 |

### 7. Critique by name

**DEVELOPER** - closed out all 3 Round 2 objections with real, independently-reproducible root-cause fixes (not patch lists), and volunteered a 4th fix (stale local test-state bug) that wasn't even assigned, disclosing it rather than quietly working around it. The self-reported 73.3% true-holdout classifier number, arrived at by writing a fresh set and explicitly *not* iterating against it afterward, is the correct way to report a generalization ceiling honestly - my own independently-written fresh batch (77.8%, from a completely different set of invented senders/phrasing) corroborates rather than contradicts that number, which is exactly what a judge wants to see from a disclosed limitation. The one place I'd still push back slightly: "Legal-suffix normalisation - FIXED" as a headline could have added "for dotted-initialism and the broadened suffix list; slash-form abbreviations beyond S/B (e.g. S/A) remain uncovered" - a minor documentation-precision note, exactly the same class of note I made about DEVELOPER's Round 2 write-up, not a code objection.

**DESIGNER / INNOVATOR** - not re-dispatched this round per ORCHESTRATION's plan (both closed Round 2 with no remaining objections); no frontend files changed this round (confirmed via git diff), so nothing new to verify on their behalf.

### 8. Submit-now readiness (today, 22 Sep 12:00 PM)

**No blockers.** Every number DEVELOPER reported this round reproduced exactly from a clean shell in this session. All 3 Round 2 objections got real, independently-verified fixes that generalize to material I invented myself (not just re-runs of DEVELOPER's own test files) - legal suffixes across 12 fresh jurisdictions, classifier across 18 fresh sender/domain/phrasing combinations including the specific spammy-TLD-plus-attachments bug class, layout across the same 6 fresh separators with the revert reasoning independently dataset-confirmed. The one residual DEVELOPER disclosed (classifier's ~73% ceiling on open natural language with no attachments) is real, honestly reported, corroborated by my own independent test, has zero effect on the shipped/scored 520-email submission, and is an inherent property of a rules-only engine running without the (unavailable) Gemini key - an acceptable, disclosed limitation, not a defect that should block submission this close to the deadline.

STATUS: NO REMAINING OBJECTIONS

## Round 4 - JUDGE, 2026-09-22 (deadline day per idea.md was 22 Sep 2026 12:00 PM - this session ran after that timestamp; noted plainly, review done honestly regardless per instructions)

Method: read `ORCHESTRATION.md`, my own Round 1-3 (76->88->92, NO REMAINING OBJECTIONS), `reviews/developer.md` Round 4 in full (the orchestrator's live-key Gemini work, independently re-verified by DEVELOPER, STATUS: OBJECTIONS=1 - low-severity `export_replay` event-ordering non-determinism, disclosed not fixed), and `git log --oneline -15`/`-30`. Independently re-ran every required command from a clean shell rather than trusting either prior write-up's numbers. Did not print or touch `backend/.env`'s contents at any point (only `git check-ignore`/`git log --all` on the path, both read-only). Made no edits to `backend/` or `frontend/`.

### 1. Independent re-verification (this session's own run)

| Check | Expected | Got | Match |
|---|---|---|---|
| `cd backend && python -m pytest -q` | 69 passed | **69 passed** (143.5s) | YES |
| `python -m sdoc.run_all` (no `--engine`) | engine=rules, 520 emails, 454/46/20 | **engine=rules, 520 emails, 32.7s, 454 OK / 46 MISMATCH / 20 NEEDS_REVIEW** | YES |
| `PYTHONUTF8=1 python work/docker/server/score_cli.py backend/out/submission.json` | FINAL 1.0000 | **FINAL SCORE 1.0000** (s1 macro-F1 1.000, s3 defect-F1 1.000, e2e 46/46, escalation P=R=1.000, all 4 reasons 5/5) | YES |
| `python -m sdoc.export_replay` (no flag) | engine=rules, not gemini | **engine=rules, 520 emails, 6685 events, 5.0 MB** | YES |
| `git status --short frontend/public/replay/` after that export | ~490 files "changed" (ordering only, per developer.md #9) | **488 files modified**; diffed `email_004.json` programmatically (parsed both versions as JSON): all top-level fields byte-identical, event *content* multiset identical, event *order* differs (`same order: False`) - confirms developer.md's finding exactly, values never change | YES, matches disclosure |
| `git checkout -- frontend/public/replay/` | tree clean after | `git status --short frontend/public/replay/` -> 0 files; full `git status --short` (repo-wide) -> empty | YES |
| `cd frontend && npm run build` | 0 TS errors (not in the required list, ran anyway - see #4) | **0 TS errors**, 1899 modules, 323KB JS / 47KB CSS | YES |
| `git check-ignore -v backend/.env` | gitignored | matched by `backend/.gitignore:3:.env` | YES |
| `git log --all --oneline -- backend/.env` | empty (never committed) | empty | YES |

I did not spend additional live-Gemini quota myself this round. Rationale: the orchestrator ran a real 14-email live batch, DEVELOPER independently re-ran that exact batch verbatim plus a disjoint 10-email batch plus a 9-email field-by-field rules-vs-gemini value diff (zero differences) - three converging, independently-reproduced data points already exist. A third live burn would mostly re-spend shared, real, rate-limited quota (15 RPM / 500 RPD on `gemini-3.5-flash-lite`, confirmed against `hackathon_info/google-ai-limits.md` by DEVELOPER) for very little incremental confidence given how tight DEVELOPER's re-verification already was (verbatim reproduction of the orchestrator's own script, not just re-reading its printed output). I instead spent the budget on read-only cross-checks that need no key: confirmed `email_004`'s sender/subject (`docs@vitalsolutions.sg`, "REQUEST BL DRAFT _ PO 26067..."), `email_513`'s subject (contains "VALPARAISO_CHILE"), and `email_011`'s category (`GENERAL`, via the local ground-truth file, JUDGE-permitted) all match the literal claims in `README.md`/`SUBMISSION.md`'s click-by-click demo scripts exactly - a judge or teammate following the script word-for-word will see precisely what the script says they'll see.

### 2. Frontend/vercel.json diff since my Round 3 commit - a correction to this round's framing, then a full audit

The brief for this round states the new work is "all backend + docs, zero frontend changes." **That is true of DEVELOPER's actual Round 4 (Gemini) commit range, but not true of everything committed since my Round 3 sign-off** - I want to be precise about this rather than accept the framing at face value, since the brief itself told me to verify it.

- `git diff --stat edc3eaa..HEAD -- frontend/ vercel.json` (i.e. everything in DEVELOPER's actual Round 4 Gemini work plus the two doc-fix commits after it): **empty**. Zero frontend/vercel files touched. This part of the framing is exactly right, and matches `reviews/developer.md` #8's own `git show --stat edc3eaa` finding.
- `git diff --stat 9be2fa9..edc3eaa -- frontend/ vercel.json` (my Round 3 commit up to the start of the Gemini round): **8 source files + `vercel.json` (new) + the expected ~490 replay-trace refresh**: `App.tsx`, `BeadCanvas.tsx`, `FlyPanel.tsx`, `HelpOverlay.tsx`, `Sidebar.tsx`, `icons.tsx`, `styles.css`, and a new `vercel.json`. These landed in the un-numbered "ARGUS rebrand + submission polish" commits (`087d956`, `40a74fc`, `9f0ea0a`, `db3d281`) between my Round 3 close and DEVELOPER's Round 4 - i.e. after I last signed off on frontend, and before neither DESIGNER nor INNOVATOR (both closed at Round 2, per `ORCHESTRATION.md`) nor I re-reviewed them in a numbered round.
- I read every line of that diff (not just the stat) rather than taking the "already reviewed" framing on faith: it's a straightforward, low-risk rebrand-and-polish set - "Asteris"/"Drosophila Gate" -> "ARGUS" (strings only, `Logo`/`Sidebar`/`App.tsx` boot-error text/CSS header comment), playback speed remapped from 1x/2x/4x to 0.25x/0.5x/1x (a demo-usability change - slower default so a presenter has time to talk over the animation, matches the new demo scripts exactly, verified below), a `BeadCanvas` perf tweak (capped DPR 2->1.5, idle-frame throttling to ~15fps, fewer trail segments while multiple agents run - all purely cosmetic/perf, no logic change), a genuine small bugfix ("Play inbox" always restarts from the top of the visible list, not wherever you last clicked - read the diff, it's a one-line dependency-array/condition fix, does what the commit message says), a display-only reframing of the fly panel's headline number from raw "suspicion" to an intuitive "confidence %" (the code comment is explicit that "nothing about the gate's actual math changes" - verified true: `confPct = Math.round((1 - shownSusp) * 100)` is a pure display transform, the underlying `suspicion`/`threshold` values passed to `NodePopover`/`ReportView`/`Timeline`/the API payload are untouched), and a new `vercel.json` (adds the deploy config, does not modify anything that was already reviewed).
- **Verified live, not just by reading the diff**: ran `npm run build` myself this session (not one of the 5 required commands, but directly relevant to "would this break the Vercel/REPLAY demo" - see item 4 in the brief) against the full cumulative diff since my Round 3: **0 TS errors, 1899 modules, 323 KB JS / 47 KB CSS** - identical shape to every prior round's number. Nothing regressed.
- **Conclusion: no objection on substance** - I read every changed line and found nothing that breaks the REPLAY demo, contradicts CONTRACT.md, or misrepresents anything (the speed-key relabelling is even correctly reflected in `HelpOverlay.tsx` and both README demo scripts, which I cross-checked). But the *process* framing ("zero frontend changes since Round 3") undercounts real, if low-risk, frontend work that no designer/innovator/judge round explicitly re-signed on its own merits before now - flagging this as a documentation-precision note for the orchestrator log, same class of note I made about DEVELOPER's Round 2/3 headline framing, not a new code objection.

### 3. README/SUBMISSION.md/backend-README honesty audit - the crux of this round

Read all three in full (current HEAD). Cross-checked every Gemini/score/test-count claim against a command I ran myself this session or against `reviews/developer.md` Round 4's independently-reproduced evidence.

**Verdict: no claim in the current, shipped text crosses from favourable-but-true into misleading.** Specifics:

- "Score: 1.0000 on the official 520-email benchmark" (README.md) - re-verified myself this session, exact.
- "classifier and all 7 field-agents verified live on `gemini-3.5-flash-lite` across a 14-email mix (6 of them exercising all 7 field-agents) ... zero errors, every value agreeing independently with the rules engine" (README.md) - matches DEVELOPER's independent reproduction exactly (14/14 zero errors, plus their own disjoint 10-email batch, plus a 9-email field-by-field value diff with zero differences). The parenthetical "(6 of them exercising all 7 field-agents)" is a real, deliberate precision fix (commit `0e3bd63`) applied in direct response to DEVELOPER's own Round 4 nit ("could be read as all 14 hitting the field agents") - I checked the git history and confirmed this exact correction is what shipped, not left as the looser original wording.
- `pytest (69 tests)` in `SUBMISSION.md` and `python -m pytest tests -q # 69 tests` in `backend/README.md` - both re-verified this session (69 passed), and both are now consistent with each other (an earlier commit had a stale `63` in `SUBMISSION.md` while `backend/README.md` already said `69`; commit `0e3bd63` fixed the mismatch - I checked this was actually corrected, not just claimed to be).
- "Battle-tested... pushed the independent score from 76 -> 88 -> 92/100" (`SUBMISSION.md`) - matches this file's own Round 1-3 history exactly. Notably, the punchy pitch *keeps* this self-critical progression rather than hiding it - showing "we started at 76 and an adversarial reviewer found real bugs each round" is a credibility-builder, not a liability, and the team correctly kept it in the pitch instead of cutting it as a "self-inflicted QA number" (which the `cb925c1` commit message explicitly says other numbers were cut for pitch punchiness) - a good instance of *not* over-editing.
- The fly-brain "honest pitch" framing ("We could've called it 'a real fly connectome' and hoped nobody asked... it's not FlyWire, not a connectome, not real fly data") is unchanged in substance from Round 1-3 (already ruled acceptable, not re-litigated) and remains accurate against the actual code (`flybrain.py`'s own docstring/comments match verbatim).
- The deliberate move of granular caveats (Gemini-unverified-at-the-time, the `ambiguous_role` 0.375 recall, the classifier's tuned-language ceiling) out of the root README/SUBMISSION and into `backend/README.md`'s "Known gaps" - already ruled an approved, intentional design choice in Round 2/3, and I re-confirm it's applied consistently now: every specific number I went looking for in `backend/README.md` (grey-zone bucket table, `ambiguous_role` 0.375, classifier "tuned against this inbox's genre of language," the new event-ordering disclosure) is present, specific, and un-softened in substance - only the root pitch is trimmed, not the record.
- **Process observation, not a live defect** (I looked at this specifically because the brief asked me to hunt for exactly this pattern): commit `038be6c` ("present Gemini integration as shipped/production-ready, not as an untested caveat") rewrote the root README's LIVE section to say "the classifier and all 7 field-agents run on it" and rewrote `SUBMISSION.md`'s stack line to drop "since no key was available during this build" - and this commit **landed before** the actual live-key verification (`edc3eaa`, later the same session). Read at that intermediate commit in isolation, "the classifier and all 7 field-agents run on it" would have been an unverified assertion of fact stated as settled - exactly the failure mode this round's brief asked me to find. It did not ship that way: the very next commits in the same session (`edc3eaa`, then the precision fix `0e3bd63`) made the claim true and then tightened its wording to the accurate, specific, independently-reproduced form quoted above. Since only the current HEAD is what gets submitted and judged, this is not a live honesty defect - but I'm naming it plainly because the *sequence* (write the confident claim, then go make it true) is a real process risk that happened to resolve correctly this time only because the live test was run in the same sitting before the deadline. Worth a note for any future round: verify before you polish the pitch to sound verified, not the other way around, even under time pressure.

### 4. A new, real, non-blocking finding of my own: the LIVE demo has no guard against burning the live-key quota via "Process entire inbox," and the pitch docs don't warn about it

Traced this myself (not copied from developer.md) after noticing DEVELOPER's Round 4 critique-of-orchestrator section flagged the *risk* qualitatively ("a single misclick on 'process all'... could burn most or all of the day's quota") without it appearing in DEVELOPER's own numbered `STATUS: OBJECTIONS=1` list, and without any corresponding line actually landing in the docs. I verified the mechanics directly:

- `backend/sdoc/config.py:default_engine()`: returns `"gemini"` whenever a key is present and `SDOC_ENGINE` doesn't force `"rules"` - by design (this is the correct behaviour for `Pipeline()`'s default).
- `backend/sdoc/api.py`'s `POST /api/process_all` (`force_engine: str | None = Query(None, ...)`, `concurrency: int = 8`) has **no** engine guard of its own: with no `force_engine` query param (which the frontend's "Process entire inbox" button never sends - confirmed by reading `frontend/src/App.tsx`'s `processAll()`/`source.processAll()` call sites, no `force_engine` argument anywhere), it falls straight through to `Pipeline`'s `default_engine`.
- **Right now, in this exact checkout, `backend/.env` contains a real key.** So starting the backend exactly as `README.md`'s LIVE section instructs (`uvicorn sdoc.api:app --port 8000`), opening the frontend in LIVE mode, and clicking the real "Process entire inbox" button in the UI would, today, silently walk all 520 emails through Gemini at concurrency 8 with no confirmation prompt - a real risk of exhausting the account's 500 RPD / 15 RPM budget (per `hackathon_info/google-ai-limits.md`, cross-checked by DEVELOPER) in minutes, on a button any curious teammate or judge could click while poking at the LIVE mode the README explicitly invites them to try ("Drop in `GEMINI_API_KEY`... and it's live").
- `run_all.py`/`export_replay.py` (the two CLI bulk tools) were correctly hardened this exact round (`edc3eaa`) against precisely this footgun by defaulting to `--engine rules`. The identical footgun in the live HTTP API's bulk endpoint, reachable from a single UI button, was not - and neither `README.md` nor `SUBMISSION.md`'s LIVE-mode instructions carry the one-line caution DEVELOPER's own write-up said was "worth a mention," even though the CLI-tool half of the same bug class got a real code fix in this exact round.
- **Severity: low, non-blocking.** The submission that's actually deployed and demoed (Vercel/REPLAY, per both README demo scripts, which explicitly say "Open REPLAY" as step 1) never touches this code path at all - `process_all` only exists on a locally-run LIVE backend, not the static REPLAY build. This is a risk for a curious teammate or judge running LIVE mode ad hoc with a real key configured, not for the submitted/scored artifact.

### 5. Verdict on DEVELOPER Round 4's one open objection (export_replay event-ordering non-determinism)

**Documenting-not-fixing is the right call; does not block.** Reasoning, re-derived from my own repro in section 1 (not taken on faith from developer.md):
- I independently reproduced the exact symptom (488 "changed" files after a no-flag `export_replay` re-run) and independently confirmed, by parsing the JSON myself rather than eyeballing a text diff, that only event *order* changes - every field value, category, status, and defect finding is byte-identical across runs.
- Root cause (asyncio.gather completion order for the 7 genuinely-concurrent field agents) is exactly what it's diagnosed as, and a real fix (sort by a fixed key before assigning `seq`) is correctly identified as cheap and low-risk but was correctly left undone this close to the deadline for a cosmetic-only issue.
- Impact is fully bounded and already proven zero on anything that's scored or shipped: `score_cli.py` has no events at all in `submission.json` (re-verified 1.0000 this session), and the **already-committed** `frontend/public/replay/traces/*.json` that ships in the Vercel build is a fixed, static snapshot - a judge loading the deployed REPLAY build never triggers a re-export, so they will never see this non-determinism at all. It is only visible to someone who re-runs `export_replay` themselves, at which point `backend/README.md`'s "Known gaps" section (verified present, current wording) already tells them why and that it's intentional (real concurrency, not faked ordering).
- The one-line philosophical note in `backend/README.md` ("forcing a fixed order would mean faking the very thing the pitch stakes itself on - that the graph reflects real, not staged, execution") is, on reflection, the correct call and not just a convenient excuse: this project's entire honesty pitch rests on "every node is a real call, nothing is decoration," and imposing a fixed event order on genuinely concurrent work would be a small but real step toward staging the very timeline the pitch says isn't staged. Leaving it as a disclosed, understood, zero-impact cosmetic quirk is more consistent with the project's own stated values than silently forcing determinism would be.
- Recommendation if there's ever a Round 5: the cheap fix (sort by a fixed `FIELDS` key before `seq` assignment) is still worth doing eventually since it removes reviewer friction (a ~490-file diff that looks alarming and isn't), but it is correctly not deadline-critical.

### 6. Updated rubric

| Category (weight) | R1 | R2 | R3 | R4 | Delta | Why |
|---|---|---|---|---|---|---|
| Accuracy (20) | 9 | 9 | 10 | 10 | 0 | Re-verified exactly (1.0000, 454/46/20); unaffected by this round's backend changes (rate limiter/engine-default/docs touch nothing in the `rules` scoring path, confirmed by re-running from scratch) |
| Spec coverage (15) | 8 | 9 | 9 | 9 | 0 | Unaffected; no spec-surface work this round |
| Reliability / HITL (15) | 7 | 9 | 9 | 9 | 0 | Unaffected; gate/HITL code untouched this round |
| Architecture / innovation (15) | 8 | 9 | 9 | **10** | +1 | The one residual that had capped this category since Round 1 (Gemini path implemented-but-never-run-live) is now genuinely closed: independently re-verified by DEVELOPER via verbatim reproduction of the orchestrator's own batch plus a disjoint batch plus a field-by-field value cross-check (zero diffs), and I independently spot-checked the specific email claims (sender/subject/category) named in the pitch's own demo script against the real dataset. Real production concerns (rate limiting, exponential backoff, the bulk-tool footgun fix) were also added and verified, not just the smoke test itself |
| UX / demo (15) | 8 | 9 | 9 | 9 | 0 | No regression (0 TS errors, cumulative frontend diff since Round 3 read line-by-line and rebuilt clean); the new `process_all`/live-key demo-safety gap (section 4) is real but low severity and doesn't touch the actual submitted/demoed REPLAY path, so it's a caution for the log rather than a further deduction this round |
| Robustness (10) | 5 | 7 | 9 | 9 | 0 | Unaffected; the S/A legal-suffix gap I flagged as a non-blocking note in Round 3 was in fact fixed this round (`normalize.py`, verified working) - a nice bonus, not scored separately since it was never a numbered objection |
| Docs / reproducibility (10) | 7 | 9 | 9 | 9 | 0 | Test-count staleness and the Gemini-claim precision nit (both from DEVELOPER's own Round 4 review) were actually fixed, not just acknowledged (verified via git diff of `0e3bd63`); the event-ordering non-determinism is a new, honestly-disclosed, zero-impact caveat that offsets what would otherwise be a further improvement - net unchanged |
| **Overall** | **76** | **88** | **92** | **93** | **+1** | 10x2 + 9x1.5 + 9x1.5 + 10x1.5 + 9x1.5 + 9x1 + 9x1 |

### 7. Critique by name

**DEVELOPER/orchestrator (this round's work)** - the live Gemini verification is real, not decorative: independently re-derived by a second party (DEVELOPER re-verifying the orchestrator) via verbatim script reproduction rather than trusting printed output, and I found nothing in a third independent pass (reading the diffs, re-running the regression suite, spot-checking the specific emails the pitch script names) that contradicts either write-up. The bulk-tool default-engine fix is a genuine, well-motivated bugfix for a footgun that is now live and real on this machine (a real key sits in `backend/.env`), not busywork. One gap: the identical footgun exists in the live HTTP API's `process_all` endpoint (reachable from a single UI button) and was not fixed or even documented with a one-line caution, despite DEVELOPER's own write-up naming the risk in prose. This is real, low-severity, and does not touch the submitted REPLAY artifact - see section 4.

**DEVELOPER Round 4 (independent re-verification role)** - the strongest verification round I've read on this project: reproduced the orchestrator's exact script rather than re-reading its output, ran an independently-invented disjoint batch, and did a field-by-field value diff the orchestrator's own script never attempted. Finding the export_replay non-determinism by literally noticing an unexpected `git status` during otherwise-routine verification (rather than going looking for it) and then diagnosing root cause rather than just reporting the symptom is exactly the standard this project has held itself to since Round 1. My only addition: the `process_all`-with-live-key risk they named in prose (section 9's closing paragraph) was real and verifiable enough that I'd have made it a numbered objection rather than a closing remark - a minor process note, not a disagreement on substance.

**DESIGNER/INNOVATOR** - not re-dispatched this round (both closed Round 2, no frontend logic changed since; the frontend work that did land between my Round 3 and this round is small, well-explained rebrand/perf/demo-usability polish that I read line-by-line myself in section 2 and found sound).

### 8. Submit-now readiness (session run after the stated 22 Sep 2026 12:00 PM deadline - noted per instructions, review done exactly as if before it)

**No blockers.** Every required number reproduced exactly from a clean shell this session: 69/69 tests, 520-email run at 454/46/20, FINAL SCORE 1.0000, `export_replay` defaults to `rules` (confirmed, not gemini), and the resulting replay-directory diff is exactly the disclosed, zero-value-impact event-ordering cosmetic non-determinism (independently re-derived, tree restored clean). Frontend/vercel.json changes since my Round 3 sign-off were real but small, sound, and rebuilt clean (0 TS errors) - I read every line rather than trusting the "zero frontend changes" framing at face value, and I stand behind the substance even though the framing undercounted it. The README/SUBMISSION honesty audit that was this round's specific ask turned up no live misleading claim - the one process observation (a confident claim written slightly ahead of its own verification, then corrected before it shipped) resolved itself within the same session and isn't visible in the current text. The one new finding I did not inherit from anyone else - `process_all`'s live-key/no-guard risk plus the missing one-line demo-script caution - is real, low severity, and does not touch the deployed/demoed REPLAY artifact.

STATUS: OBJECTIONS=1
1. [Low, non-blocking] The live HTTP API's `POST /api/process_all` has no engine guard against defaulting to `gemini` whenever a real key is present (the frontend's "Process entire inbox" button sends no `force_engine`), unlike `run_all.py`/`export_replay.py` which were correctly hardened to default to `rules` this exact round. A real key currently sits in `backend/.env` on this machine. Neither `README.md` nor `SUBMISSION.md`'s LIVE-mode instructions carry a one-line caution against clicking that button with a live key configured, even though DEVELOPER's own Round 4 review named this risk in prose. Does not affect the submitted/demoed REPLAY artifact (the demo scripts correctly use REPLAY throughout); relevant only to a teammate or judge who runs LIVE mode locally on this exact checkout and clicks "Process entire inbox." Fix (either is sufficient, neither is required before submission): add one line to the LIVE section of `README.md`/`SUBMISSION.md` ("don't click Process entire inbox with a live key configured"), or make `process_all` require an explicit `force_engine=gemini` to ever use Gemini (mirroring the CLI tools' now-safe default).

### 9. Addendum - objection #1 fixed, closed (commit `e6eba91`)

Read the fix myself in `backend/sdoc/api.py` rather than taking the commit message on faith:

```python
async def process_all(force_engine: str | None = Query(
        "rules", pattern="^(rules|gemini)$",
        description="Bulk endpoint (all 520 emails) - defaults to 'rules' even if a key is configured, "
                    "same reasoning as run_all/export_replay's CLI default: an operator clicking "
                    "'process entire inbox' in LIVE mode should not silently spend a live key's whole "
                    "daily quota. Pass ?force_engine=gemini to opt in explicitly.",
        ), concurrency: int = 8):
```

- Confirmed directly (not just by reading the diff): `inspect.signature(process_all).parameters['force_engine'].default` -> `Query(rules)` in a live Python session. Default really is `"rules"` now, not `None`.
- Exactly mirrors the `run_all.py`/`export_replay.py` fix pattern from earlier this same round, applied to the one place that pattern hadn't reached yet - closes the gap at the root (server-side default) rather than only in a doc caution, which is a stronger fix than either of the two options I suggested.
- Confirmed the coordinator's "zero frontend impact" claim myself: read `frontend/src/data/source.ts`'s `processAll()` - `fetch('/api/process_all', { method: 'POST' })`, no query string, no `force_engine` ever sent by the frontend for this call (`process()`/`retry()` are the only methods that ever set `force_engine`, and only when the caller explicitly passes `opts.forceEngine`). So "Process entire inbox" in the UI was always going to hit whatever the server-side default is - it now safely gets `rules` regardless of whether a key is configured, with zero code or behavior change needed on the frontend side.
- Re-ran the full suite myself: `cd backend && python -m pytest -q` -> **69 passed** (143.8s), matching the coordinator's claim exactly, no regression from this one-line change.
- Did not re-run the full 520-email `run_all`/`score_cli` chain for this addendum - the fix touches only `api.py`'s bulk HTTP endpoint (never called by `run_all.py`, `export_replay.py`, or `score_cli.py`), so it cannot affect the scored submission; re-verifying the same 1.0000 a third time in one round would not add information.
- The one thing I did not independently re-verify live: actually starting `uvicorn` and hitting `POST /api/process_all` with no query string to watch it report `engine=rules` in real trace events end-to-end (as opposed to reading the function signature and the test suite passing). Given the fix is a one-line default-value change in a `Query(...)` declaration, directly inspected in a live interpreter, and the full test suite still passes, I judge this level of verification sufficient for a low-severity, purely-defensive fix this close to the deadline - noting the gap in method for the record rather than silently upgrading my confidence past what I actually checked.

**Objection #1 is closed.** No remaining objections from JUDGE this round.

STATUS: NO REMAINING OBJECTIONS
