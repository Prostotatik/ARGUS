
## Round 1 - DEVELOPER (frontend half), 2026-09-21

### What I built (`frontend/`, Vite + React + TS, plain CSS, no chart libs)
Kept the previous agent's scaffold (source.ts, usePlayer, graphState, GraphPanel, NodePopover, Sidebar, EmailList, icons, mock) and finished the rest:
- `main.tsx`, `App.tsx` (state, run/retry/review orchestration, run-id guard against stale streams), `styles.css` (whole theme), `hooks/useReducedMotion.ts`.
- `components/FlyPanel.tsx`: inputs -> Kenyon cells -> decision neuron, staged activation (inputs, KCs, neuron) from the `gate` event, winner KC highlight, suspicion vs threshold meter, Hebbian-update feedback (before -> after, "simulated" tag in REPLAY), input legend from backend `input_names` (mismatch / low conf / near-miss / doc), seeded KC layout, subsamples large nets (draws ~110 of 1600 KCs plus every active one; caption says so), caption "Architecture inspired by the fruit fly's olfactory system".
- `components/Timeline.tsx`: 6-step strip (Received, Classified, Field agents with 7 status dots, Aggregate+compare, Fly gate, Report ready), t+ms, engine badge + duration, skipped states for non-comparison mails.
- `components/ReportView.tsx`: headline ("No mismatch detected" when all match), SI vs BL table for the 7 fields with evidence, mismatches highlighted, gate line, classification-only card for non-comparison mails, processing-failure box with retry, human-review panel (reason text, escalation reason, source evidence, "was escalation warranted" verdict, Confirm mismatch / Confirm no mismatch / Correct a field form; per-row pencil to correct any field), fly-net feedback line. REPLAY/MOCK marks the review as "simulated locally".
- Center header with Pipeline/Report tabs, Replay/Re-run/Skip, and LIVE/REPLAY/MOCK pill; result banner under the graph ("Review" pulses amber for NEEDS_REVIEW).
- Fixes to inherited code: circular-JSON crash in `simulateReview` (report event payload references the result), mock report payloads copied, compare fields prefer the (possibly human-corrected) result over stale event payload, MISMATCH label colour, sidebar wrap.
- `README.md` in `frontend/`.

### Verification (evidence)
- `npm run build` (tsc -b && vite build): 0 TS errors, 1894 modules, built OK.
- Claude-in-Chrome extension was NOT connected, so I drove the installed Chrome 153 headless over CDP (own script in scratchpad, no deps) at 1536x1024, 1280x800, 1024x768, 390x844 and read screenshots. Compared with the reference: sidebar, graph with curved glowing connectors + particle trails, 7 real field pills, incoming emails, fly panel with lit decision neuron, bottom timeline all match the reference layout/style at 1536x1024.
- Exercised in MOCK mode: OK email, MISMATCH email, NEEDS_REVIEW (errored node) email, non-comparison (spam) bypass arc, popover, report, review confirm -> status flips to MISMATCH, list dot + stats update, fly feedback "0.83 -> 0.92 simulated".
- Exercised LIVE mode against a throwaway fake FastAPI-contract server (Node, scratchpad, now stopped): /api/health detection, Vite proxy, SSE parsing over fetch stream, streamed node lighting, NEEDS_REVIEW -> POST /api/review -> updated report, 32-input / 1600-KC fly net with 80 active KCs. Console: no errors except the expected 502/404 on `/api/health` when no backend is running (that is the auto-detect probe).
- Dev servers, fake server and headless Chrome stopped.

### Deviations from the reference
- Sidebar copy is honest, not the reference numbers: "Emails to process", "Shipments compared", "Discrepancies found", "Accuracy vs. labels" (offline self-eval, `-` when absent); top issues are real defect fields, padded with zero-count fields.
- Added: filter chips + search on the email list (520 emails), gate chip node between Aggregator and Report, Pipeline/Report tabs, mode pill says LIVE/REPLAY/MOCK instead of just "Live".
- Fly panel big number is suspicion (0-1) with threshold, not "98.2% confidence"; input labels are group legend + tooltips because 32 inputs cannot carry text labels at this size.
- Timeline shows t+ms from trace events (relative) instead of wall-clock times.
- Under 820px the graph scrolls horizontally at a readable scale instead of shrinking to illegibility.

### Known gaps
- Not run against the real backend or real replay files: `backend/out` and `frontend/public/replay/` were empty while I worked. The adapters accept several index.json shapes (array or {emails|items|results}; per-row `result`, `result_summary` or inline fields) but this is unverified against the real export.
- REPLAY: retry and review are simulated (clearly marked); the fly-net update there is a heuristic (x0.82 / x1.08+0.02), not the real Hebbian rule.
- No automated tests; verification was visual + build. No live Gemini path exercised (no key).
- ~100 KC drawn out of 1600 by design; the drawn set is a fixed stride sample plus active KCs.
- Chrome extension unavailable: cross-browser (Firefox/Safari) untested.

### CONTRACT objections / requests (none blocking)
1. Report event `payload` must not embed `events` (circular / huge); please keep events only at the Result top level of `traces/<id>.json`.
2. Request `POST /api/review` response (or `GET /api/flybrain`.history[-1]) to carry `{before, after, verdict}` in a stable shape; I read `history[-1]` after a review.
3. SSE framing is unspecified; I accept `data: {json}\n\n` and bare NDJSON. Please keep one JSON Trace event per message and no named `event:` types.
4. `/api/retry/{id}`: please emit fresh `start`/`done|error` events for the retried nodes and a new `report` event; frontend folds by last-event-per-node.
5. `flybrain.input_names` (already in `flybrain.py`) is used for the legend: please keep it in the replay `flybrain.json`, and keep `kc_active` indices in `[0, n_kc)`.
6. `index.json` rows: please include `from`, `subject`, `received_at`, `category`, `status`, `defect_fields` per row (list dots/chips rely on them).

STATUS: NO REMAINING OBJECTIONS


## Round 1 - DEVELOPER (backend half), 2026-09-21

### What I built (`backend/`, package `sdoc`)
Kept the previous agent's `config.py`, `docparse.py`, `labels.py`, `normalize.py`, `ocr.py` (read fully, sound; only behaviour found by scoring was changed) and finished the rest:
- `rules_extract.py` (rules field agent), `classifier.py` (rules classifier), `llm.py` (Gemini classifier + field agents, structured JSON, evidence verification), `intake.py` (SI/BL role assignment + preflight: missing_attachment / wrong_doc_type / unreadable), `compare.py` (pure compare + decide), `flybrain.py` (numpy gate), `pipeline.py` (7 parallel field agents via asyncio.gather, per-node events, per-node error + retry), `store.py` (results + human review + fly update), `api.py` (all CONTRACT endpoints, SSE, CORS open), `run_all.py`, `export_replay.py`, `submission.py`, `tools/score_local.py` (dev-only), `tests/` (62 pytest), `README.md`, `requirements.txt`, `.env.example`.
- Replay artifacts written to `frontend/public/replay/` only (index.json, traces/*.json x520, stats.json, flybrain.json, submission.json; 4.4 MB; no ground-truth labels; `accuracy_vs_labels` is one offline number).

### Score (rules engine, no Gemini key on this machine; `python work/docker/server/score_cli.py backend/out/submission.json`)
- FINAL 1.0000 = 0.30 * stage1 macro-F1 1.000 + 0.20 * stage3 defect-F1 1.000 + 0.50 * end-to-end 46/46 (1.000).
- Classification 520/520 (all five categories P=R=1.00). Field-level F1 1.000, exact-match 1.000 over 114 comparable pairs (txt, pdf, docx, xlsx all parsed).
- Reliability: escalation recall 20/20, precision 20/20; per reason 5/5 wrong_doc_type, 5/5 missing_attachment, 5/5 unreadable (image-only PDFs flagged, no OCR engine installed), 5/5 missing_value. `python backend/tools/score_local.py --errors` -> "0 emails differ from the reference".
- Error classes found on the way (fixed at the root, no id special-casing): (a) first run scored 0.9606: 3 true MISMATCH emails were sent to NEEDS_REVIEW by the gate because a PDF label with CJK junk (`TOTAL Gross Weight | II | (KGS)`) matched only fuzzily and the container-table detector false-matched "NADU 600102" in an address -> exact-synonym-in-any-variant rule, stricter container-row regex, fuzzy penalty 0.2 -> 0.12; (b) defect_fields ordering (scorer uses sets; submission is sorted anyway); (c) the gate treated `engine_fallback` + a benign formatting note as novel -> fallback is now familiar context in calibration and only low-confidence + fallback counts as an anomaly.
- Caveat: the dataset is synthetic with regular templates. 1.000 says the pipeline matches this generator's semantics, not that it will hold on a messy real inbox. Dev perturbation check (89 txt pairs re-rendered with MT / lbs / `KGS.00`, upper/title case, LTD->LIMITED): 0 false alarms, 0 missed defects. Unit tests cover the same normalisation.

### Fly gate: what it is and what it measurably does
- 32 inputs -> 1600 Kenyon cells (50x, 3 inputs each = 9% connectivity) -> APL-style global inhibition + top-80 WTA (5%) -> one decision neuron with plastic KC weights; verdict-gated LTD/LTP; weights persisted to `backend/out/flybrain_state.json`; interpretable (`kc_active`, `winner_kc`, per-input `drivers`). Wording everywhere: "architecture inspired by the fruit fly's olfactory system".
- Calibration (seeded, no labels): unsupervised familiarity on 2500 synthetic normal outcomes, threshold from held-out normals (0.08), evaluated on 10 synthetic anomaly kinds never trained on: held-out normal false-escalation 0.000, anomaly recall 0.995 (worst kind low_conf_only 0.95). Design lesson recorded in `flybrain.py`: with plain sparse coding a lone input could not be told apart from its familiar combinations; coincidence-detecting KCs + an input floor lifted recall from 0.66 to 0.995.
- On the real 520 emails the gate alone escalates 0 results (max suspicion 0.0003 over 109 comparisons); all 20 NEEDS_REVIEW are deterministic triggers (the net still scores them, 0.63-0.89, so the demo shows the pattern). So the gate cannot have hurt accuracy here, and it has ZERO measured contribution to the score either. It matters when confidence drops (Gemini path, OCR, typo-like near misses) or after human teaching. The pitch must not claim the gate raised the score.

### Objections / notes on ORCHESTRATION and CONTRACT design
1. idea.md says NEEDS_REVIEW is "exactly where the fly-brain gate's job is measured". In the labelled data every NEEDS_REVIEW case is deterministic (no attachment / wrong type / unreadable / blank) so the gate is not measured by any scored axis. Suggest the pitch demos the gate on the synthetic anomalies (calibration report in flybrain.json) and on live human teaching, not on the score.
2. The bundle labels treat a BL_COMPARISON email with no attachments that only asks for the draft BL as status OK, but the "attachments dropped" wording as NEEDS_REVIEW/missing_attachment. I follow the labels via a body-wording rule (`intake.py`, documented in README; Result has `comparison_performed:false`). Arguably that should be a review too; it is a data-semantics call the orchestrator may want to state in the pitch.
3. CONTRACT `status: null` for non-comparison vs `submission.json` needing a status: submission maps null -> "OK" (sample shape). Documented.
4. `received_at` does not exist in the dataset; it is simulated (deterministic 7-minute arrival stream). Frontend should not present it as real time.
5. Retry state (`RunState`) is in memory only; a server restart makes `/api/retry` return 409. Fine for a demo, not for production.
6. Frontend requests honoured: report payload never embeds `events`; SSE = one `data: {json}` message per Trace event; retry emits fresh start/done|error for only the retried nodes + downstream aggregator/compare/gate + a new `report`; `input_names` kept in flybrain.json/API and `kc_active` in [0,1600); `/api/review` response has `review.fly = {before, after, verdict, n_kc_updated}` and `GET /api/flybrain` `history[-1]` has `{before, after, verdict}`; index.json rows carry `from, subject, received_at, category, status, defect_fields` plus nested `result`. Extra Result keys are additive (see backend/README.md).

### Critique of the frontend half (verified by reading code and data, not by running the UI)
- `frontend/src/data/source.ts normalizeRows` reads `it.result ?? it.result_summary ?? inline`: matches my index.json (checked by reading). Not run in a browser by me.
- Objection: REPLAY review shows a heuristic fly update (x0.82 / x1.08) labelled simulated; the real rule exists in the backend. Keep the label, and do not present those numbers as the real Hebbian update; alternatively I can export a real precomputed learning demo.
- Objection: the sidebar "Accuracy vs. labels" shows 1.0 in replay (real, offline, synthetic-data number). Caption it as offline self-eval on the synthetic set so a judge does not read it as production accuracy.

### Verification evidence
- `python -m pytest backend/tests -q` -> 62 passed (compare normalisation, classifier rules, fly learning direction/locality/persistence/calibration, pipeline event order + parallel field starts, missing_attachment / wrong_doc_type / unreadable / missing_value, injected failure + retry, Gemini path with a fake client, Gemini failure fallback, no-key-never-claims-gemini, API SSE + review + 404/422).
- `python -m sdoc.run_all` -> 520 emails in ~18 s; submission keys/shape identical to `work/bundle/sample_submission.json`.
- API smoke via FastAPI TestClient (health, process SSE, result, retry after `?inject_fail=field:consignee,field:port_of_loading`, review, flybrain, stats, emails). Not run under a real uvicorn socket + browser.

### Known gaps
- Gemini path is written against the installed google-genai 2.18 API and tested only with a fake client; NOT verified against the live API (no key). Default model `gemini-2.5-flash` (override `SDOC_GEMINI_MODEL`).
- No OCR engine installed and no vision-LLM path: scanned PDFs are flagged `unreadable` (matches the labels, but a real read would be better UX).
- Classifier rules and label synonyms are tuned to this inbox's language; container size/type is not compared (spec compares count only); the gate has only been calibrated on synthetic vectors.

STATUS: OBJECTIONS=2
1. Gemini path unverified against the live API (needs a key and one real run; fallback-to-rules keeps the system working regardless).
2. Fly gate is validated only on synthetic anomalies and contributes nothing measurable to the official score; it needs real grey-zone data (Gemini/OCR runs, human verdicts) before any accuracy claim.

## Round 2

Method: read ORCHESTRATION.md, CONTRACT.md, idea.md, the reference PNG, and all four Round-1 reviews in full. Reused JUDGE's own perturbation harnesses (`reviews/judge_scratch/*.py`) as an independent, already-adversarial regression suite (not written by me, so not something I could have tuned to) alongside my own new grey-zone eval script. Re-ran `pytest`, `run_all`, `score_cli`, `export_replay`, and `npm run build` after every meaningful change, not just at the end. Found and fixed a real infinite-loop bug in my own xlsx rewrite via direct timing bisection before it could ship (see #3).

### Judge findings

**1. Fly gate inert on real data / circular calibration — FIXED (capability), grey-zone job now measured.**
- Kept deterministic triggers (missing_attachment/wrong_doc_type/unreadable) going straight to NEEDS_REVIEW (`pipeline.py`, unchanged).
- Routed genuinely uncertain cases through the gate: (a) low per-field confidence and near-miss values were already wired into `build_input_vector` and now actually reach the gate on real data because OCR (item #5) recovers 2 of the 3 real scanned emails well enough to extract from but not confidently — `email_513`/`email_514` now escalate via `decided_by: "flynet"`, not a rule (verified: `python -m sdoc.run_all` then inspect `out/results.json`). (b) classifier confidence is now routed through the gate for **every** category, not just BL_COMPARISON (`pipeline.py` non-comparison branch) — 0 impact on the real 520 emails (verified: no email has confidence low enough to trigger it) but exercised and tested (`tests/test_pipeline.py::test_low_confidence_non_comparison_can_escalate`). (c) added `ambiguous_role` as one of the held-out eval's grey-zone categories (SI/BL role assignment losing its confident title-based signal).
- New held-out grey-zone eval, `backend/tools/grey_zone_eval.py` (not the circular synthetic-vector calibration report, not the 520-email score): real SI/BL pairs from the bundle's own clean `.txt` attachments, 5 hand-written mutations, run through the full pipeline. Numbers (40 pairs/bucket, seeded, reproducible):

  | bucket | gold escalate | gate escalated (flynet) | rate |
  |---|---|---|---|
  | clean | no | 0/40 | 0.0 |
  | clean_defect | no | 0/40 | 0.0 |
  | near_miss_typo | yes | 25/40 | 0.625 |
  | conflicting_line | yes | 35/40 | 0.875 |
  | ambiguous_role | yes | 15/40 | 0.375 |

  precision 1.00, recall 0.625, F1 0.769 — full numbers written to `backend/README.md` and `backend/out/grey_zone_eval.json`. This is the non-circular measurement the gate previously lacked; it does not feed the official score (verified: `score_cli.py` still reports 1.0000 after this file was added).
- Verification: `python -m tools.grey_zone_eval` (from `backend/`), numbers above reproduced live in this session.

**2. Gate escalation drops confirmed defects and mislabels review_reason — FIXED.**
- `pipeline.py`: when the gate escalates an already-decided `OK`/`MISMATCH` result, `has_defect`/`defect_fields` are now kept from what `compare.decide()` already established (previously wiped to `false`/`[]`). `submission.json` is unaffected (it still zeroes them for NEEDS_REVIEW rows, matching the bundle's own label semantics for those rows — verified `score_cli.py` unchanged at 1.0000).
- Honest reason: a pure gate escalation with nothing missing/unreadable now uses `review_reason: "low_confidence"` (new, 5th value) instead of lying with `missing_value`. **Design note (the spec question flagged for this task):** the required `sample_submission.json`/scoring schema only has 4 slots (`wrong_doc_type|missing_attachment|unreadable|missing_value`) and scoring's `score_reliability` only buckets by the **gold** truth's `review_reason`, never checks the submission's — so extending the live enum has zero scoring risk. `submission.py` maps `low_confidence -> missing_value` **only at the submission-export boundary** (documented in-code and in CONTRACT.md) so the shipped file stays schema-compliant while the live Result/API/UI never lie about the real reason. I judged this more honest than silently keeping `missing_value` everywhere, and safer than inventing a 5th value in the exported schema the organizers' scorer was not built to expect.
- `many_mismatches` (>=3 confident field differences) is no longer, by itself, an anomaly signal (`flybrain.py build_input_vector`) — it only counts when at least one of those mismatches is itself shaky (near-miss or low-confidence). A confirmed, clean multi-field defect is reported as `MISMATCH` with all its fields, never gated away. Verified: `clean_defect` bucket above = 0/40 false escalations.
- Verification: `pytest -q` 63 passed; `score_cli.py` unchanged 1.0000; grey-zone eval `clean`/`clean_defect` buckets 0/40 false escalations.

**3. Rules engine overfit/brittle — FIXED (broadly), a few items declined with reasons.**
- Classifier: fresh-phrasing probe (JUDGE's own 23 cases, `reviews/judge_scratch/cls.py`) now 23/23 (was 18/23) — added generic content signals (booking-instruction wording, statement-of-account/outstanding-balance, acknowledgement/no-action wording, prize/phishing/CEO-fraud/SEO-spam wording) rather than more sender/subject-template hard-coding. Did **not** remove the sender-domain-vs-TLD nudge (judge's sub-suggestion (d)) — assessed removing it as a real regression risk on the live 520-email set without time to validate against every real sender domain in the bundle; declined for this round, logged here rather than silently left as claimed-done.
- Layouts: added `Label - value`, `Label = value`, markdown-table rows, and (label-exact-only, see perf note below) separator-free `Label value` lines (`rules_extract.py split_line`); added 4+-column and transposed (header-row-of-labels + value-row) tables for both docx and xlsx (`docparse.py`). Verified with JUDGE's own harnesses: `run_a.py` (10 layout perturbations) 0/84 changed each; hand-built docx-transposed/docx-4-col/xlsx-transposed cases all extract every field correctly (shown in this session's tool output).
- Synonyms/legal suffixes/countries: `S/B`->`SDN BHD`, `L.L.C.`/`FZ-LLC` variants, leading `PT`/`THE` prefixes, a parenthetical/trailing local-registration qualifier (`(S)`, `, Dubai`) that is dropped only when it is a recognised short locale tag (never an arbitrary parenthetical, to avoid conflating e.g. `X (Middle East) FZE` with plain `X`); full `pycountry` country-name database (~250 names) unioned with a small abbreviation list, replacing the old ~90-country hard list — fixes Iran/Iraq/Colombia/Argentina/Ecuador and any other ISO country JUDGE's list happened not to enumerate; `PORT OF X`/`X PORT` wrapper stripping, bare-UN/LOCODE-suffix pulling, trailing-country-word stripping without a comma, `HCMC`/`PTP`/`KLANG` aliases; reversed container order (`40'HC x 6`). Verified: JUDGE's own adversarial `unit_norm.py` (138 cases, none written by me) 125/138 passing (was ~112/138 by my count of JUDGE's list against the unpatched code). Remaining 13: one pair in JUDGE's own list is internally contradictory (`ROXCEL TRADING GMBH` vs `...GMBH & CO KG` is listed as both a required-match and a required-non-match once punctuation is normalised — cannot satisfy both, left as the more conservative "not equal"); the rest are pre-existing design trade-offs inherited from Round 1, not Round 2 regressions (an empty legal-suffix/qualifier set is always treated as a subset of any set, so e.g. `SAFQA` vs `SAFQA LIMITED` or `SINGAPORE` vs `SINGAPORE (JURONG)` "match" — necessary for the many legitimate generic-vs-specific cases, a real precision/recall trade-off not fixed this round given the regression risk of changing it under time pressure); container size/type is intentionally not compared (locked scope decision, idea.md).
- OCR-noise tolerance: near-miss similarity floor relaxed from 0.88 to 0.72 when either side's extraction is flagged `ocr` (`compare.py`), since OCR introduces 1-2 character noise (verified against real OCR output on `email_512`/`513`/`514`, e.g. "SDN BHD" vs OCR-misread "SDN SHD").
- **Performance regression found and fixed before it shipped:** my first pass at the dash/equals/markdown separators made every line containing a dash (addresses, vessel refs, date ranges — most lines in a real document) trigger the label-matching fuzzy fallback (`difflib.SequenceMatcher` against ~90 synonyms). `labels.py match_label` gained an `exact_only` param; the new separator paths use it (real labels always match exactly; fuzzy matching non-label dashed text was never useful and was the expensive part). Separately, direct per-email timing (`asyncio.wait_for(..., timeout=15)` bisection over the first 15 emails) caught a genuine **infinite loop** in my own xlsx transposed-table rewrite (`docparse.py _read_xlsx`, a missing `i += 1` in the fallback branch) before this reached `run_all`/the submission — fixed, `python -m sdoc.run_all` is back to 520 emails in ~33-37s (was 17.8s in Round 1; the increase is the extra generalisation work — layouts, synonyms, OCR-capable paths — not a lingering bug, re-verified with per-email profiling after the fix).
- Verification: `pytest -q` 63 passed; `score_cli.py` unchanged 1.0000; `run_a.py`/`cls.py`/`unit_norm.py` numbers above; new regression coverage added directly in `tests/test_pipeline.py` (`test_low_confidence_non_comparison_can_escalate`) rather than a separate messy-input test file, given time budget — declining JUDGE's specific suggestion of porting `run_a..d.py` wholesale into `tests/`, since they already exist and run cleanly as `reviews/judge_scratch/*.py` and duplicating them risks drift between two copies; noted as a partial on that specific sub-ask.

**4. Mid-value truncation → silent false MISMATCH — FIXED.**
- `docparse.py`: a plain-text attachment that does not end with a newline is flagged `ParsedDoc.truncated` (verified: all 192 real `.txt` attachments in the bundle end with `\n`, so this is a safe, generic, content-free signal — not tuned to any specific label text).
- `rules_extract.py extract_field`: when the document is truncated and a field's evidence line sits on/near the very last line, it is flagged `possibly_truncated`.
- `compare.py compare_field`: `possibly_truncated` on either side forces `state: "missing"` (never a confident value), regardless of what the partial number/name looks like — it flows into the existing, already-correct `missing_value` NEEDS_REVIEW path instead of a silent wrong MISMATCH.
- Verification: JUDGE's own `run_d.py` `cut_mid_weight` case (BL truncated mid `Gross Weight: 2`) — was a false MISMATCH on all 22 clean pairs (Round 1); now 40/40 correctly escalate to `NEEDS_REVIEW`/`missing_value` (shown in this session's tool output, reused JUDGE's harness unmodified). Binary-format truncation (pdf/docx/xlsx cut mid-file) was already safe via the existing "file fails to parse -> unreadable" exception path — unaffected, still correct.

**5. Scanned PDFs flagged unreadable but never read — FIXED (real OCR, not a stub).**
- Added `rapidocr-onnxruntime` (pure Python + onnxruntime, ships its own small detection/recognition ONNX models in the wheel — **no system Tesseract binary needed**, verified working fully offline in this sandbox) as the primary OCR engine in `ocr.py`, with Tesseract kept as a fallback if present. `SDOC_OCR=off` disables both, falling back to the honest `unreadable` label.
- Fixed a genuine thread-safety bug found while wiring this in: lazy engine construction set a "tried" flag before construction finished, so a second attachment parsed concurrently (SI/BL are parsed in parallel via `asyncio.to_thread`) could see a still-`None` engine and silently fall back to "OCR unavailable" for that one document. Fixed with a proper lock (`ocr.py _RAPIDOCR_LOCK`).
- Verified against the real dataset: of the 5 real `unreadable` emails, 3 (`email_512/513/514`) are genuinely scanned 21KB image PDFs — OCR now reads all 3 (real party names, ports, weights recovered, minor character-level noise as expected of OCR); 2 (`email_511/515`) are deliberately corrupt (non-image, garbage-byte) PDFs, correctly still `unreadable` regardless of OCR. `email_512` still lands on a deterministic `missing_value` (one field OCR did not recover); `email_513`/`514` now escalate via the **gate itself** (`decided_by: flynet`), not a rule — the real, non-circular grey-zone case item #1 asked for.
- Verification: `pytest -q` 63 passed (test expectations updated to the new, correct counts: `unreadable: 4, missing_value: 6` instead of `5/5`, with a comment explaining why); `score_cli.py` unaffected (1.0000 — `review_reason`/`decided_by` are not scored fields); direct OCR output shown in this session's tool calls.

**6. Gemini path unverified live / unthrottled — PARTIALLY FIXED (code done, live key still unavailable).**
- `llm.py GeminiClient`: added a shared `asyncio.Semaphore(SDOC_GEMINI_CONCURRENCY)` (default 3) across all calls made through one client (classifier + 7 field agents), so a comparison email's up to 8 Gemini calls are never all in flight at once. Exponential backoff with jitter on failure (`0.8*2^attempt` normally, `4.0*2^attempt` on a detected 429/RESOURCE_EXHAUSTED/rate-limit response, honouring a server-suggested retry delay when present in the error text); `max_attempts` raised from 2 to a configurable default of 4 (`SDOC_GEMINI_MAX_ATTEMPTS`).
- Declined/not possible this round: an actual live smoke test — **still no `GEMINI_API_KEY` available in this environment.** This remains unverified against the real API, stated plainly rather than claimed. The rules-engine fallback path (unaffected by this change, still what produces every number in this repo) keeps the system fully working regardless.
- Verification: `pytest -q` 63 passed (existing fake-client Gemini tests unaffected by the added semaphore/backoff, since they use `max_attempts=1` explicitly).

**7. Timeline "all 7 match" on skipped compare — FIXED.**
- `Timeline.tsx`: the "Aggregate + compare" step's subtitle now checks `nodes.compare?.state === 'skipped'` directly (was checking `nodes.aggregator?.state`, which is never `'skipped'` on the no-compare path — the aggregator still emits `start`/`done` there for its own preflight summary, only `compare` itself is skipped) and prints `"not run"` instead of computing `bad ? ... : 'all 7 match'` off an empty `fields` array (which always fell through to the false "all 7 match" claim). Also guards on `cmp && cmp.length` so a truly empty comparison never says "all 7 match".
- Verification: `npm run build` 0 TS errors; logic verified by reading the exact event sequence in `pipeline.py _no_compare` (`aggregator` done, `compare` skipped) against the fixed condition.

**8. HITL panel below the fold / no-op default verdict — FIXED (root-caused, not just cosmetically).**
- **Root cause of the no-op**, found by re-deriving the math rather than just moving buttons: KC->decision weights start at 1.0 ("everything is novel"), and the LTP rule (`w += eta_pot*(1-w)`) applied to a weight already at 1.0 is mathematically a no-op — *any* "escalation was correct" confirmation of a never-before-touched pattern reads as `0.63 -> 0.63` regardless of which verdict the UI defaults to. Fixed by starting weights at `DEFAULT_INIT_W = 0.9` instead of 1.0 (`flybrain.py`, applied in both `__init__` and `calibrate()`'s reset) — "everything is novel" is preserved (0.9 still saturates almost immediately), but there is now real headroom for the very first LTP confirmation to show a visible, honest change.
- Separately, and more fundamentally: deterministic-trigger escalations were being taught to the gate at all, even though the gate never decided them (`decided_by: "deterministic_trigger"`) — `store.py apply_review` now only calls `gate.learn()` when `decided_by == "flynet"`; for a deterministic trigger it returns `review.fly = {skipped: true, reason: "..."}` instead, surfaced honestly in the UI ("No fly-net weight update: this escalation was a deterministic trigger...") rather than a misleading `before == after` line. Mirrored in the REPLAY/mock local simulation (`App.tsx`) for consistency across modes.
- Panel placement: `ReportView.tsx` now renders the human-review section **immediately after the headline**, above the SI/BL comparison table, whenever a review is actually needed (`needsReview`) — it used to always come after the 7-row table + gate line, which is what pushed it below the fold at 1536x1024. The "correct a field" / "already resolved" cases (not urgent) keep the original after-the-table position.
- Verification: `npm run build` 0 TS errors; backend math re-derived and unit-testable (`flybrain.py` LTP formula with `DEFAULT_INIT_W=0.9`: `0.9 + 0.5*(1-0.9) = 0.95`, a real, visible change); `pytest -q` 63 passed (existing flybrain tests are directional, not exact-value, so unaffected by the init-weight change).

**9. Docs/pitch — FIXED.**
- Root `README.md` added: what this is, the honest fly pitch (explicitly not claiming a real fly brain / FlyWire), how to run LIVE and REPLAY, current score numbers, what is real vs. simulated, known limitations, and a ~3-minute demo script.
- Reference tagline "Smarter checks. Smoother trade." replaced in the sidebar footer (`Sidebar.tsx`) with "Compare with confidence. Escalate when unsure." — accurate to what this build actually does (compare SI/BL, escalate on uncertainty), kept it as a two-line footer tagline in the same visual slot rather than removing it, since the slot/art was DESIGNER's approved work and the ask was to fix the wording, not the layout.
- Simulated timestamps: `EmailList.tsx` already labelled this (Round 1); added the same honest labelling (visible "(simulated)" text + tooltip, not just a title attribute nobody hovers) to `NodePopover.tsx` and `ReportView.tsx`'s header, the two places it was shown unlabelled.
- `reviews/innovator.md` already exists (INNOVATOR wrote it in Round 1 after JUDGE's Round 1 objection about it) — nothing to do here.
- Verification: `npm run build` 0 TS errors; root README reviewed for accuracy against the actual numbers reproduced in this round.

**10. Classifier low-confidence never escalates / failure-retry not reachable from UI — FIXED.**
- Classifier confidence now routed through the gate for every category (see item #1) — ties the two findings together as the task instructions asked.
- UI affordance for a processing failure: verified the existing "Retry failed nodes" button already calls the real `POST /api/retry/{id}` endpoint in LIVE mode (`App.tsx retry()` -> `source.process(id, ..., {retry:true})` -> `source.ts` routes to `/api/retry/` when `retry:true`) — this was already correct, per INNOVATOR's note. What was missing (JUDGE's literal complaint) was a way to **produce** a failure from the UI in the first place, not just recover from one: added a "Simulate failure" button next to Re-run (LIVE mode only, next to the selected email) that calls the same real pipeline with `inject_fail=classifier` — a real API mechanism (already tested, `tests/test_pipeline.py::test_injected_failure_is_visible_and_retry_repairs`) now reachable without typing a query string.
- Verification: `npm run build` 0 TS errors; backend mechanism already covered by existing passing tests; button wiring traced through `App.tsx` -> `data/source.ts` -> `POST /api/process/{id}?inject_fail=classifier`.

### Designer items

**#1 engine badges — VERIFIED, still functions.** Did not touch `GraphPanel.tsx`'s `runDefaultEngine`/`OddTag` logic (INNOVATOR's Round-1 implementation). Confirmed present and unchanged; `npm run build` clean; the `Result` type gained an optional `review` field this round (additive), which does not interact with the engine-badge logic.

**Tablet dead space (<=1180px) — DECLINED, one-line reason.** Out of the reference design's primary 1536x1024 target; investigated briefly (the `.right` column's uncapped email-list height stretches the shared grid row taller than the graph needs at that breakpoint) but a real fix risks a visual regression at the primary target under this round's time budget, so left as DESIGNER left it, per the explicit permission to decline a non-cheap fix outside the primary target.

### Innovator items

**#1 expose `eta_dep`/`eta_pot`/`tau` in `FlyBrain.public()`/replay `flybrain.json` — IMPLEMENTED.** `flybrain.py public()` now includes `eta_dep`, `eta_pot`, `tau`, and `init_w` (additive, CONTRACT.md updated). Verified: `python -m sdoc.export_replay` then read `frontend/public/replay/flybrain.json` -> `{"eta_dep": 0.6, "eta_pot": 0.5, "tau": 12.0, "init_w": 0.9}` — matches (and now backs) the frontend's own hardcoded fallback constants exactly, so `App.tsx`'s `fly.eta_dep ?? ETA_DEP` reads the real number with zero frontend changes needed for this item.

**#2 per-KC before/after weights directly in `POST /api/review`'s response — IMPLEMENTED.** `flybrain.py learn()` now returns `kc_active`/`kc_weights_before`/`kc_weights_after` alongside the existing `before`/`after`/`verdict`; `store.py`'s `review.fly` (already at the top level of the Result INNOVATOR asked for) carries them straight through. `frontend/src/data/source.ts LiveSource.review()` now reads this directly instead of a second `GET /api/flybrain` call (falls back to the old two-call path only if a backend build predates this field, for compatibility). Verification: `npm run build` 0 TS errors; `pytest -q` 63 passed (`test_api_sse_review_and_flybrain` exercises `POST /api/review` end to end).

### Process notes / assumptions logged (DESIGNER/INNOVATOR not running this round)

- Kept all INNOVATOR delight features untouched (particle trails, count-up, autoplay, fly canvas, Hebbian viz, keyboard shortcuts) — verified by reading the relevant components before and after my edits and confirming no overlap; `npm run build` stayed clean throughout.
- `CONTRACT.md` updated for every wire-shape change (review_reason 5th value + submission mapping, has_defect/defect_fields kept on gate escalation, non-comparison NEEDS_REVIEW, `review.fly` shape, `flybrain.json` new fields) — all additive, nothing existing removed.
- Declined further generalisation of the sender-domain/TLD spam heuristic (judge.md #3(d)) without time to validate against the full real inbox's sender list — logged under item #3 above rather than silently claimed done.
- Did not port `reviews/judge_scratch/run_a..d.py` into `backend/tests/test_messy.py` verbatim (judge.md #3(e)) — added one new pytest case directly instead and continued using the existing scratch harnesses (unmodified, so they stay useful as an independent check) for the broader sweep; a full port was judged lower value than the time it would cost given the harnesses already run cleanly against the shipped code.

STATUS: NO REMAINING OBJECTIONS
Backend + frontend code correctness/completeness against idea.md and the hackathon spec, from my own remit: all 10 judge findings addressed (8 fixed, 1 partially fixed with an honest reason — Gemini live verification blocked by no available key, not a code gap, 1 fixed via a documented, non-scoring-risk design decision on the review_reason enum); both designer items resolved (1 verified, 1 declined with reason per the explicit permission to do so); both innovator items implemented. `pytest` 63/63, `score_cli.py` 1.0000 (unchanged through every fix), `npm run build` 0 TS errors, all re-verified after the final change in this round.
