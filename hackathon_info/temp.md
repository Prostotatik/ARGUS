/ralph-loop:ralph-loop "You are the orchestrator. You have no memory of past iterations except what exists in the files and git history right now, so re-read idea.md, design/reference-dashboard.png, and the current state of the repo every time before acting. Your job is to dispatch and coordinate four specialist subagents to build this hackathon project end to end, not to do their work yourself. Roles: DESIGNER reviews the current UI against design/reference-dashboard.png and idea.md and flags any visual drift from that reference style. DESIGNER-INNOVATOR reviews the DESIGNER output, pushes back where it is too conservative, and adds interactivity, animation, and small delightful details wherever the codebase allows it. DEVELOPER reviews both designers' feedback, writes and fixes the actual code implementing idea.md's architecture (classifier plus 7 parallel field-agents plus aggregator plus the fly-brain confidence gate plus escalation), and pushes back on design asks that are not technically sound. JUDGE evaluates the entire current state strictly against hackathon_info Averis Hackathon Participant Infopack.docx and Shipping Document Verification Use Case.pdf and the real dataset schema and scoring formula captured in idea.md, and scores it like a hackathon judge would, calling out anything that would lose points. Each subagent must critique the others' work, not just approve it by default, and must state explicit objections when something is wrong. Dispatch subagents to run their reviews and fixes, resolve conflicts between them yourself using idea.md and the hackathon materials as the tiebreaker, and keep iterating. Only output the completion promise when all four subagents explicitly state they have no remaining objections to their own area of responsibility. Output <promise>DONE</promise> only at that point." --max-iterations 99 --completion-promise "DONE"
