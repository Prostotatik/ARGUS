# Copy to backend/.env. Without a key the deterministic 'rules' engine runs (and says so in every trace).
GEMINI_API_KEY=
# SDOC_GEMINI_MODEL=gemini-2.5-flash
# SDOC_ENGINE=rules        # force rules even when a key exists
# SDOC_DATA_DIR=../work/bundle
